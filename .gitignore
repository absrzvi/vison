# ── Tool caches ───────────────────────────────────────────────────────────────
.mypy_cache/
.ruff_cache/
.pytest_cache/
__pycache__/
*.py[cod]

# ── Node / frontend ───────────────────────────────────────────────────────────
node_modules/
control-centre/dist/

# ── Claude Code (machine-specific settings and installed framework skills) ─────
.claude/settings.local.json
.claude/launch.json
.claude/skills/
!.claude/skills/bmad-security-sentinel/
!.claude/skills/bmad-security-sentinel/SKILL.md
!.claude/skills/bmad-security-sentinel/checklist.md

# ── BMAD agent runtime (installed tooling, not project artifacts) ──────────────
_bmad/
!_bmad/custom/config.toml
!_bmad/custom/config.user.toml
!_bmad/custom/bmad-agent-dev.toml
!_bmad/custom/bmad-dev-story.toml

# ── Superpowers brainstorm scratchpad ─────────────────────────────────────────
.superpowers/

# ── Design upload artifacts (binary assets, not source) ───────────────────────
claude-design-mockups/uploads/
claude-design-mockups/preview/

# ── ÖBB brand assets / portal design files (binary, not source) ───────────────
current-portal-design/

# ── Large binary / archive files ──────────────────────────────────────────────
*.zip
*.png
*.jpg
*.jpeg
*.gif
*.webp

# ── OS noise ──────────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini

# ── Python virtual envs ───────────────────────────────────────────────────────
.venv/
venv/
env/

# ── Secrets / env files ───────────────────────────────────────────────────────
.env
.env.*
!.env.example

# ── SQLite databases (runtime state, not source) ──────────────────────────────
*.db
*.sqlite
*.sqlite3

# ── Hailo AI model files (large binary, deployed separately) ──────────────────
*.hef
models/

# ── Test & build artifacts ────────────────────────────────────────────────────
*.log
.coverage
*.egg-info/
test-results/

# ── Local tooling & generated artifacts (not source) ──────────────────────────
/package.json
/package-lock.json
/node_modules/
lint.json
parse_lint.py
openssh
tappas_check/
*.docx

# E10-S2: weekly alert-effectiveness reports are generated CI artifacts, not source
cloud-backend/reports/
