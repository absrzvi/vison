---
baseline_commit: a7e6a2d
---

# Story 11.3: Profile management + preferences re-key migration

Status: review

<!-- Created 2026-06-15 via bmad-create-story (Amelia). Third story of Epic 11 (Control Centre Admin & Identity).
     Source: _bmad-output/planning-artifacts/epics.md §"Epic 11" → "E11-S3 — Profile management + preferences re-key migration".
     Depends on 11-1 (DONE, JWT foundation: get_current_user, CurrentUser{user_id,username,role}, users table 0009) and
     11-2 (DONE, user_audit table 0010, liveness extractors). Both in _bmad-output/implementation-artifacts/.
     Review tier (A2): FULL adversarial wire-replay — migration on EXISTING data. Permission tier: Tier 3.
     SCOPE IS SMALLER THAN THE EPIC IMPLIED — see "Decisions" D1: 11-1 ALREADY swapped preferences.py to key by
     current_user.user_id and the frontend already uses Bearer auth. The residual work is the SCHEMA re-key migration
     (the table column is still plain TEXT holding the old API-key string; rows read 404 today) + the FK + the Profile
     screen. create-story did not re-scope the epic's intent; it grounded it against what 11-1 actually shipped. -->

## Story

As **a logged-in operator**,
I want **my preferences (critical-alert threshold, staleness threshold) stored against *my* user identity and editable from a dedicated Profile screen**,
so that **my settings follow me regardless of which deployment key was used before — and an operator can only ever read or write their own row, never another user's.**

## Context — why this story exists

E2-S8 shipped `operator_preferences` ([0002_operator_preferences.py](../../cloud-backend/migrations/versions/0002_operator_preferences.py)) with `operator_id` as a **plain `TEXT` primary key**. Before auth existed, the client sent a free-text `operator_id` and the row was effectively keyed by **the shared API-key string** — every "operator" was the same key, so there was never any real per-user preference data.

**11-1 already did half of E11-S3's job.** When 11-1 cut the routers over to JWT, it repointed `preferences.py` to key by `current_user.user_id` ([preferences.py:63,127](../../cloud-backend/src/cloud_backend/routes/preferences.py)) and left a docstring saying *"Existing rows keyed by the old shared API-key string read as 404 → graceful defaults until E11-S3 re-keys/backfills them (Alembic migration). The data migration is E11-S3's job; this story only swaps the identity source."* ([preferences.py:5-9](../../cloud-backend/src/cloud_backend/routes/preferences.py)). So **the identity-source swap the epic describes is already shipped** — the GET/PATCH already use the token's `user_id`. What is **not** done:

1. The **schema is still wrong**: `operator_preferences.operator_id` is a free-floating `TEXT` PK with no relationship to `users`. Nothing stops a stale/garbage key from being inserted, and there is no referential integrity tying a preference row to a real user.
2. The **old rows are dead weight**: any pre-cutover rows are keyed by the defunct API-key string and will never be read again (no token's `user_id` will ever equal that string).
3. There is **no Profile screen** — preferences live in a gear-button modal in the header ([OperatorPreferences.jsx](../../control-centre/src/components/shell/OperatorPreferences.jsx)), not under an authenticated-user profile route.

This story closes all three: a re-key migration (drop the defunct rows, retype/constrain `operator_id` as a real FK to `users`), and a Profile screen route surfacing the existing controls.

## Decisions (locked — review before dev)

> **CREATE-STORY GROUNDING (2026-06-15, Amelia).** The epic text was written before 11-1 shipped. Four facts diverge from the epic and are surfaced here as Decisions rather than silently following the epic (the create-story divergence rule). **D1 and D3 are load-bearing and may warrant a one-line user/pre-flight confirm; D2, D4, D5 are mechanical corrections with an obvious right answer.**

> **PARTY-MODE RESOLUTION (2026-06-15, Winston + Amelia + Mary + Freya).** All five Decisions resolved; one genuine clash (D5 `unattended_threshold_min`) argued and settled. Verdicts:
> - **D1 → DROP confirmed, but make the deletion LEGIBLE** (Mary's rider, Winston-endorsed): a why-it's-safe comment block atop `0011`, a `log.warning` emitting the deleted row count, and a docstring note that downgrade restores schema **not** rows. Silent deletion is the only unacceptable version.
> - **D3 → in-place DELETE→ALTER→ADD-FK confirmed.** Winston verified the whole migration runs in ONE transaction ([env.py](../../cloud-backend/migrations/env.py) wraps `run_migrations()` in `connection.begin()`) → atomic rollback, no half-migrated state. `USING operator_id::uuid` is **required** (Postgres has no implicit text→uuid cast in `ALTER COLUMN TYPE`), not "pointless on an empty table" — story wording corrected. Keep `ON DELETE CASCADE` but document why (Winston: revisit if prefs ever hold audit-relevant data). Assert the PK survives the ALTER. Amelia: **no existing test raw-inserts `operator_preferences`** (grepped — zero hits), so the FK breaks nothing today; the AC3 test must seed a genuinely **non-UUID-castable** TEXT value (not a random UUID-string) or it proves nothing about the DELETE-before-ALTER ordering.
> - **D4 → `api_key` STAYS, comment-fix only — confirmed.** Add a **tracked** Phase-2 follow-up (shared static key → per-producer service identity) so a security reviewer sees "known and scoped," not a surprise (Mary).
> - **D5 → CLASH RESOLVED.** Extract `SegmentedControl` (Amelia confirmed it's already fully decoupled from the modal's focus-trap → zero regression risk; render-inline is the *more* awkward option). **Profile ships ONLY the two server-backed controls** (alert + staleness) under a username/role header. **`unattended_threshold_min` stays on the gear-modal only** — Freya initially argued for modal-parity-with-a-"this device" cue, then **conceded to Amelia**: do not seat a control that breaks the "your settings follow you" promise on the one screen named after persistence. It graduates onto Profile as a real synced field when the backend column lands (a clean follow-up, NOT this story).
> - **AC tightenings (Amelia, 3 — all would otherwise cause a review bounce):** AC4 browser-verify must **clear localStorage between logout/login** so the re-read proves *server* persistence not stale local cache; AC5 add the explicit "valid-token-no-row still 404→defaults" carve-out so it doesn't read as contradicting the preserved E2-S8 contract; AC2 assert the two DB CHECKs via `pg_constraint`/`information_schema`, **not** via an app-layer 422 that never reaches the DB.
> - **Type-seam note (Winston):** `PreferencesOut.operator_id` / `PreferencesPatchOut.operator_id` stay typed `str` — `users.user_id` is `UUID(as_uuid=False)` so asyncpg returns a string; a reviewer "fixing" it to `UUID` would introduce a serialization wobble. Record this so it isn't re-litigated.

- **D1 — The re-key is a DROP/SEED, not a mapping (epic-endorsed, restated with teeth). ✅ RESOLVED (party-mode) → DROP, made LEGIBLE.**
  The epic already calls this: *"the safe PoC choice is to **drop/seed** preferences against real `user_id`s (there is no real per-user data to preserve — every 'operator' was the same shared key), rather than attempt a fabricated mapping. Document this in the story as a Decision; do not silently migrate."* ([epics.md:2486](../../_bmad-output/planning-artifacts/epics.md)). **Confirmed: the migration DELETEs all pre-existing `operator_preferences` rows** (they are keyed by the dead API-key string; there is no honest mapping from one shared key to N distinct users — any mapping would *invent* per-user data that never existed). New rows are created on-demand by the existing PATCH upsert, now keyed by a real `user_id`. **No data is fabricated.** AC3 pins that the migration does exactly this and nothing more.
  - **LEGIBILITY RIDER (Mary, Winston-endorsed):** "the migration deletes data" is the exact phrase that makes a pilot/security reviewer's head snap up — so the act must be *legible*, not silent. The migration MUST: (a) carry a **comment block** at the top of `0011` stating why the DELETE is safe (rows keyed by the retired shared API-key string, no per-user data, drop/seed per D1, **irreversible**); (b) emit a **`log.warning` with the deleted row count** at upgrade time, so the deletion is in the migration record; (c) state in the **docstring** that downgrade restores the *schema* but **not** rows. The data is worthless, but the audit trail of having deleted it *deliberately* is what a reviewer actually buys. (T1, AC3.)

- **D2 — Migration number is `0011`, NOT `0010` (epic is stale).**
  The epic names this migration **0010** ([epics.md:2486,2503](../../_bmad-output/planning-artifacts/epics.md)), but **11-2 already shipped `0010_user_audit.py`** ([migrations/versions/0010_user_audit.py](../../cloud-backend/migrations/versions/0010_user_audit.py)). 11-3's migration is **`0011_preferences_rekey_user_id.py`** with `down_revision = "0010"`. (This is the same class of "epic snapshot vs shipped reality" the payload-audit rule guards — caught here for migration numbering.)

- **D3 — How to re-type `operator_id` to a real FK, given the PK type changes (the migration-design decision).**
  Today `operator_id TEXT PRIMARY KEY` ([0002:21](../../cloud-backend/migrations/versions/0002_operator_preferences.py)). Target: `operator_id` is a `UUID` FK → `users.user_id`. The `users.user_id` column is `UUID(as_uuid=False)` ([0009:29](../../cloud-backend/migrations/versions/0009_users.py)). **Because we DROP all existing rows first (D1), the column-type change is unconstrained by data** — the cleanest, lowest-risk shape is:
  1. `DELETE FROM operator_preferences` (drop the defunct rows — D1).
  2. `ALTER COLUMN operator_id TYPE UUID USING operator_id::uuid` — the `USING` clause is **REQUIRED, not optional**: Postgres has no implicit text→uuid cast in `ALTER COLUMN TYPE`, so the ALTER errors on the schema change itself *regardless of row count* without it. (Earlier story drafts called it "pointless on an empty table" — wrong; corrected by Winston. It is needed for the cast to be legal; it is *safe* because the DELETE leaves no rows to fail the cast.)
  3. `ADD CONSTRAINT fk_operator_preferences_user FOREIGN KEY (operator_id) REFERENCES users(user_id) ON DELETE CASCADE`. **`ON DELETE CASCADE`** so deleting a user (11-2 has no hard-delete today, but the FK should be honest) cleans up their preference row; at minimum this prevents an orphan. The PK stays on `operator_id` (the 0002 inline `primary_key=True` PK, auto-named `operator_preferences_pkey`, survives the retype). **CASCADE doc rider (Winston):** the migration docstring must state *why* CASCADE — "chosen so a future user-delete can't orphan a prefs row; revisit if prefs ever hold audit-relevant data" — because the day a hard-delete/GDPR-erase path is added, CASCADE silently vaporizes a user's prefs with no second look. Acceptable for a 2-integer PoC row; documented so it's a deliberate choice, not an accident.
  **Downgrade** drops the FK **first**, then `ALTER COLUMN ... TYPE TEXT USING operator_id::text`, restoring the 0002 shape (rows are not restored — they were dropped; downgrade returns the *schema*, consistent with how 0009/0010 downgrades work). **In-place ALTER beats drop-and-recreate** — it preserves the two CHECK constraints (`ck_threshold_sec_valid`, `ck_staleness_threshold_sec_valid`) and the column defaults *for free*, without re-declaring them (less surface, less drift from 0002). **✅ Confirmed in-place (party-mode).**
  - **Atomicity (✅ Winston-verified):** the whole migration runs in ONE transaction — [migrations/env.py](../../cloud-backend/migrations/env.py) wraps `context.run_migrations()` in `connection.begin()` — so a mid-flight failure rolls back cleanly; **there is no half-migrated state.** This was the load-bearing correctness worry (Failure Scenario 2) and it holds.
  - **Migration-safety (cloud-backend/CLAUDE.md "Migration safety"):** the only locked table is `operator_preferences` itself (a ~0–few-row PoC table that nothing streams from) — **no lock on `events`/`alerts`** and no concurrent-read hazard on a hot table. The `ALTER COLUMN TYPE` takes an ACCESS EXCLUSIVE lock on `operator_preferences` only, momentarily, on an empty table. Acceptable.
  - **Type-seam (Winston):** `PreferencesOut.operator_id` / `PreferencesPatchOut.operator_id` stay typed **`str`** ([preferences.py:36,42](../../cloud-backend/src/cloud_backend/routes/preferences.py)) — `users.user_id` is `UUID(as_uuid=False)` so asyncpg returns the value as a `str` already; the `str` field is correct and **no model change is needed**. Do NOT "fix" it to `UUID` (introduces a serialization wobble). Recorded so a reviewer doesn't re-litigate.

- **D4 — `api_key` / `require_api_key` is NOT fully removed here — the `config:19` comment is stale (11-1 D1 overrode it). ✅ RESOLVED (party-mode) → STAYS, comment-fix + tracked follow-up.**
  `config/__init__.py:17-19` says `api_key` is *"Removed when E11-S3 lands."* That was written by 11-1 **before** 11-1's own Round-1 review carved `POST /api/v1/events` (the machine-to-machine ingest) **back onto `require_api_key`** (11-1 D1: *"unattended machine producer, human JWT wrong model; the ONE documented AC5 exception"*). `require_api_key` is still **live on `routes/ingest.py:29`** ([routes/ingest.py](../../cloud-backend/src/cloud_backend/routes/ingest.py) imports it). **Decision: do NOT remove `api_key`/`require_api_key`.** This story only removes the *preferences-keying* reason for it (which is now gone). **Action (T5):** correct the two stale comments — `config/__init__.py:17-19` (drop "Removed when E11-S3 lands"; state it stays for the ingest service-token per ADR-23) and `preferences.py:5-9` (the re-key it promised is now done).
  - **TRACKED-FOLLOW-UP RIDER (Mary):** a shared static API key with no rotation and no per-producer identity is line one of any ÖBB security questionnaire — not a blast-radius risk for *this* PoC (~3 operators, no PII, raw video never leaves the train), but a reviewer *will* ask. Removing the service-token entirely is a Phase-2 "proper service identity" story; **record it as a tracked follow-up** (a one-line entry in `deferred-work.md` and/or an ADR-23 note), so the answer to "what about that shared key" is "known, scoped, tracked" — a checkbox, not a flinch. (T5.)

- **D5 — Profile screen REUSES the existing preference controls; it does NOT rebuild them. ✅ RESOLVED (party-mode, clash settled).**
  `OperatorPreferences.jsx` (the gear-modal, [shell/OperatorPreferences.jsx](../../control-centre/src/components/shell/OperatorPreferences.jsx)) renders three `SegmentedControl`s wired to `useFleetData()`, which PATCH through the Bearer-authenticated `api/preferences.js`. The Profile screen is a **route** (`/dashboard/profile`) surfacing the alert + staleness controls under the authenticated user's identity (`username` + `role` from `useAuth()`).
  - **Extract `SegmentedControl` — confirmed, near-zero risk (Amelia).** `SegmentedControl` ([OperatorPreferences.jsx:10-72](../../control-centre/src/components/shell/OperatorPreferences.jsx)) is a pure self-contained function — it owns its own `focusIdxRef`/`groupRef` and has **zero references to the modal**; the modal's focus-trap ([:84-114](../../control-centre/src/components/shell/OperatorPreferences.jsx)) operates on `panelRef` via a `document` keydown listener and does **not** care whether the control is imported or local. Moving it to a shared file (`shell/SegmentedControl.jsx` + `formatSec`) and importing it back is a literal cut-paste; render-inline is the *more* awkward option (it duplicates the keyboard-nav the story says not to duplicate). **Guard:** extract only if the modal's existing tests stay green with zero changes to the focus-trap code (surgical-changes rule, Mary). Keep the modal's backdrop / `aria-modal` / Escape-Tab trap / toast on the modal.
  - **Keep BOTH the gear-modal and the Profile route — confirmed (Freya).** Not "confusing duplication": one is a quick-access overlay for an operator mid-incident (nudge a threshold without leaving the Live board), the other is the canonical home — a normal shell pattern (menubar vs Settings). Whether the gear later deep-links to `/profile` is Freya's epic-close unification call, **not this story**.
  - **Minimal Profile (Freya):** an identity header (`username` + a role pill from `useAuth()`) above the two reused preference rows, on a `--obb-surface` card, with loading/error/populated states. **No avatar, no edit-profile, no password-change, no account metadata** — those are surfaces Freya designs in the epic-close pass; faking them now reads worse than their absence.
  - **`unattended_threshold_min` STAYS OFF the Profile screen — CLASH RESOLVED (Amelia's framing won; Freya conceded).** The control is localStorage-only: FleetContext PATCHes `{unattended_threshold_min}` ([FleetContext.jsx:244](../../control-centre/src/context/FleetContext.jsx)) but the backend `PreferencesPatch`/`PreferencesOut` models have only `threshold_sec`+`staleness_threshold_sec` ([preferences.py:48-50](../../cloud-backend/src/cloud_backend/routes/preferences.py)) — the PATCH silently default-drops it; the GET never returns it. The Profile screen's *entire thesis* is "your settings follow you regardless of deployment key" (the literal story statement) — so seating a control that demonstrably does NOT follow you there, **even under a "Saved on this device" cue**, launders a contradiction onto the one surface that can't afford one. **Decision: Profile ships ONLY the two server-backed controls; `unattended_threshold_min` stays on the gear-modal only** (its localStorage behavior there is pre-existing and tolerable). It **graduates onto Profile as a real synced field when the backend column lands** — a clean follow-up (new column + model field), explicitly NOT this story. AC4 requires only the two server-backed controls anyway. (Freya initially argued modal-parity-with-a-cue, then conceded: parity isn't worth quietly breaking the persistence promise on the persistence screen.)

## Acceptance Criteria

**AC1 — two users have fully independent preferences (the core isolation guarantee)**
Given two distinct logged-in users A (`user_id` U_A) and B (`user_id` U_B), when A `PATCH /api/v1/operators/me/preferences {threshold_sec: 90}` and B `PATCH {threshold_sec: 30}`, then A's `GET` returns `threshold_sec: 90` and B's `GET` returns `threshold_sec: 30` — **A's write never touches B's row and vice versa**. Identity comes **only** from the token's `user_id`; the request body carries no `operator_id` and any client-supplied `operator_id` is ignored (the endpoint has no such body field — Security Test 1).

**AC2 — `operator_preferences` is keyed by a real `user_id` FK; the old TEXT-key shape is gone**
After migration `0011`, `operator_preferences.operator_id` is `UUID` and has a FOREIGN KEY to `users.user_id` (`ON DELETE CASCADE`), and the original PK survives the retype. Verified by a **catalog schema assertion** (query `information_schema`/`pg_constraint`): the column type is `uuid`, a FK constraint referencing `users(user_id)` exists, the PK still exists, AND **both named CHECK constraints (`ck_threshold_sec_valid`, `ck_staleness_threshold_sec_valid`) are still present in the catalog**. **(Tightened, Amelia):** prove CHECK survival by querying the catalog directly — do NOT infer it from an app-layer 422 (a `threshold_sec=45` PATCH 422s at the Pydantic boundary via `_VALID_THRESHOLD`, which never reaches the DB and therefore proves nothing about the DB CHECK surviving `ALTER COLUMN`). A PATCH for a `user_id` that exists in `users` succeeds.

**AC3 — migration 0011 applies cleanly + idempotently on a DB containing pre-migration rows; the drop/seed Decision is executed exactly + legibly (D1)**
Given a DB at `0010` with at least one pre-migration `operator_preferences` row keyed by a **genuinely non-UUID-castable TEXT value** (e.g. `operator_id='shared-api-key-abc'`, NOT a random UUID-string — Amelia: a UUID-string would let an ALTER-before-DELETE bug pass undetected), `alembic upgrade head` **DELETEs that row** (D1 — no fabricated mapping), retypes the column, and adds the FK, with NO error from the string→uuid cast (**the DELETE precedes the ALTER** — this is the load-bearing ordering; an empty-table test would hide the bug). The upgrade emits a **`log.warning` with the deleted row count** and the migration carries the why-it's-safe comment block (D1 legibility rider). The migration is idempotent under the standard downgrade→upgrade re-apply test (mirror 11-1/11-2's strengthened migration test); the whole migration is one transaction (atomic rollback on failure — D3). `down_revision = "0010"`. Downgrade restores the `TEXT` column + drops the FK first (rows are not restored — D3).

**AC4 — Profile screen renders, round-trips, and persists across logout/login (browser-verified)**
Given an authenticated session, a Profile screen at `/dashboard/profile` renders loading / error / populated states, shows the user's `username` and `role` (from `useAuth()`), and surfaces **only the alert-threshold + staleness controls** (NOT `unattended_threshold_min` — D5); committing a change PATCHes and the new value is reflected. After logout → login as the same user, the server value is read back — the committed `threshold_sec` survives a fresh session. **(Tightened, Amelia):** because `FleetContext` initializes thresholds *from localStorage* then reconciles via a background GET, "logout→login→value persists" can falsely pass off stale localStorage even if the server write failed. So the browser-verify MUST **clear localStorage (or use a fresh browser profile) between logout and login**, so the re-read comes *only* from the server GET — proving *server* persistence, not local cache. **Browser-verified** per [control-centre/CLAUDE.md](../../control-centre/CLAUDE.md): golden path (change a threshold → clear localStorage → re-login → value persists *from server*) + one edge (logged-out access blocked OR the PATCH-error toast).

**AC5 — a logged-out user cannot read or write preferences (401)**
Given no token (or an invalid/expired token), `GET` and `PATCH /api/v1/operators/me/preferences` both return `401` (ADR-10 envelope), not a default-filled 200 and not a 500. **(Carve-out, Amelia):** a *valid* token with no saved row still returns the **404→defaults** envelope preserved from E2-S8 ([preferences.py:66-77](../../cloud-backend/src/cloud_backend/routes/preferences.py); the frontend depends on this — [api/preferences.js:34](../../control-centre/src/api/preferences.js) maps 404→defaults) — so 401 is **only** for absent/invalid/expired tokens, while 404 is the valid-token-no-row path. (Stated so AC5 / Security Test 3 don't read as contradicting the preserved 404 contract.) The Profile route is behind `RequireAuth` (unauthenticated → `/login`), matching the existing dashboard guard.

**AC6 — gates green; no regression to the already-shipped preferences path**
cloud-backend unit + integration (real Postgres, testcontainers) cover AC1–AC3 and AC5 at ≥80%; `ruff` + `mypy --strict` clean on touched `src/`; control-centre `vitest` green for the Profile surface; the existing gear-modal preferences path still works (its tests stay green — D5 must not regress the modal's focus-trap). The full cloud-backend integration suite passes on real Postgres (the migration applies inside the existing Alembic-head fixture without breaking the other modules' `seed_auth_users` path).

## Security Tests (RED-first — write before domain tests, per dev-story DoD)

Write each to fail for the right reason first, then implement:

1. **No client-supplied identity — A cannot write B's row by manipulating the request:** A's token + a body that *attempts* to carry `operator_id: U_B` (or `operator_id` in any form) does NOT write B's row — the field is ignored (not in the model); only `current_user.user_id` keys the upsert. Assert B's row is untouched. (This is AC1's adversarial twin and the epic's named Security Test.)
2. **SQL-safety on the user-id key path:** the `user_id` flows into the parametrized query as a bound param (`:oid`), never string-formatted. A `user_id`-shaped value with SQL metacharacters cannot escape the bind (the column is UUID-typed post-migration, so a non-UUID `sub` 422s/401s before it reaches SQL — pin that a junk `sub` claim does not reach an unparametrized path). Confirm the existing `text(...)` calls keep `:oid` binding.
3. **Logged-out / invalid-token → 401 on both verbs (AC5):** no token, expired token, tampered token → 401 on GET and PATCH — never 200-with-defaults, never 500.

## Integration test (A1 hard gate + A3 real-path seeding)

**`test_preferences_isolation_two_users`** (testcontainers real Postgres) — MUST run and pass before the story flips to `review` ("deferred to CI" is not acceptable; `docker info` to confirm, start it if down). This is the E10-A1 gate.

**A3 — seed via the REAL path; use TWO FRESH operators, NOT the conftest synthetics (Amelia).** Create two users through the **real `create_user` flow** (the same bcrypt/insert the app uses — reuse 11-2's `create_user`, NOT a raw INSERT of a hand-computed hash), mint a Bearer for each via the real `create_access_token`, then drive **A PATCH 90 → B PATCH 30 → A GET == 90 → B GET == 30 → assert exactly two rows, each FK-valid to its user**. **Do NOT reuse the two fixed synthetic UUIDs in `conftest.py`** (operator + admin, [conftest.py:26-27](../../cloud-backend/tests/integration/conftest.py)): (a) the isolation test wants two *operator peers*, not operator-vs-admin (else "can A touch B's row" muddies with "can an admin touch anyone's"); (b) `seed_auth_users` inserts those rows with `password_hash="x"` via raw psycopg2 ([conftest.py:74](../../cloud-backend/tests/integration/conftest.py)) — reusing them would satisfy the FK but **skip the real-bcrypt path A3 exists to prove**. Create two fresh operators via `create_user`.
**FK negative test (Amelia confirmed safe to add — grep of `tests/` for `operator_preferences` = ZERO hits, so the new FK breaks nothing today):** `test_preference_fk_rejects_unknown_user` does a **raw INSERT** of a prefs row with a random UUID (the endpoint can never produce an unknown `user_id` — `get_current_user`'s liveness check 401s first — so a raw INSERT is the only honest way to hit the FK violation).

Plus **`test_rekey_migration_drops_defunct_rows`** (AC3 — seed a `0010`-era row with a **genuinely non-UUID-castable** TEXT `operator_id` like `'shared-api-key-abc'`, NOT a random UUID-string, run `upgrade head`, assert it's gone and the column is UUID+FK and the deleted-count was logged) and the **`0010→0011` downgrade→upgrade re-apply test** (AC3 idempotency). Mirror the testcontainers fixture shape across `cloud-backend/tests/integration/` and the `seed_auth_users` idiom in [tests/integration/conftest.py:42](../../cloud-backend/tests/integration/conftest.py).

> **Note the pre-existing integration-test isolation flaky** documented in 11-1/11-2's reviews: `test_ai_quality_rates.py` intermittently fails under multi-module ordering due to `DATABASE_URL`-leaking `pg_url` fixtures — **confirmed not introduced by Epic 11** (task_c97431d4). If it surfaces, do NOT chase it here; verify the 11-3 tests are deterministically green in isolation and note it.

## Tasks / Subtasks

- [x] **T1 — Alembic `0011_preferences_rekey_user_id`** (AC2, AC3, D1, D2, D3) — new migration `down_revision="0010"`. `upgrade()`: capture row count → `DELETE FROM operator_preferences` → **`log.warning` the deleted count** (D1 legibility) → `ALTER COLUMN operator_id TYPE UUID USING operator_id::uuid` (the `USING` is REQUIRED, D3) → `ADD CONSTRAINT fk_operator_preferences_user FOREIGN KEY (operator_id) REFERENCES users(user_id) ON DELETE CASCADE`; preserve the two CHECK constraints (in-place ALTER keeps them — D3). Add the **why-it's-safe comment block** + **CASCADE-rationale + downgrade-restores-schema-not-rows docstring** (D1/D3 riders). `downgrade()`: **drop FK first** → `ALTER COLUMN operator_id TYPE TEXT USING operator_id::text` (rows not restored). **RED first:** `test_rekey_migration_drops_defunct_rows` (non-UUID-castable seed) + the downgrade→upgrade re-apply test (mirror 11-2's 0010 test). The whole migration runs in one transaction (atomic — confirmed env.py).
- [x] **T2 — Confirm/keep `preferences.py` correct; add the FK-aware negative path** (AC1, AC2, AC5, Security Tests) — `preferences.py` already keys by `current_user.user_id` and binds `:oid` (verify, do NOT rewrite). Confirm a non-UUID/garbage `sub` cannot reach an unparametrized path (Security Test 2) and that GET/PATCH 401 without a token (AC5 — already enforced by the router's `Security(get_current_user)`; pin it). No new endpoint (epic: "key-source swap, not a new endpoint").
- [x] **T3 — Frontend Profile screen** (AC4, D5) — `src/components/profile/Profile.jsx` (+CSS, `--obb-*` only) at route `/dashboard/profile` behind `RequireAuth`; show `username`/`role` from `useAuth()` (identity header) above **only the alert-threshold + staleness controls — NOT `unattended_threshold_min`** (D5 clash resolution; that control stays on the gear-modal). **Extract `SegmentedControl` (+`formatSec`)** from `OperatorPreferences.jsx` into a shared file both the modal and Profile import (D5 — it's already fully decoupled from the modal's focus-trap, so this is a literal cut-paste; do not duplicate keyboard-nav). **Guard:** the extraction must leave the modal's existing tests green with ZERO change to the focus-trap code; if that can't hold, render the body inline instead. Add a `/dashboard/profile` `<Route>` in [App.jsx](../../control-centre/src/App.jsx) (mirror the `users` route shape, but `RequireAuth` not `RequireAdmin` — every user has a profile) + a nav entry in [AppShell.jsx](../../control-centre/src/components/shell/AppShell.jsx) (all authenticated users). vitest for the Profile component states + the extracted control.
- [x] **T4 — Integration tests + browser verify** (A1 gate, AC4) — `test_preferences_isolation_two_users` (A3: two FRESH operators via `create_user`, NOT the conftest synthetics), `test_preference_fk_rejects_unknown_user` (raw-INSERT random UUID), `test_rekey_migration_drops_defunct_rows` (non-UUID-castable seed + assert deleted-count logged), the migration re-apply test — all green on real Postgres (Docker up, run locally). Browser-verify the Profile golden path **clearing localStorage between logout and login** so the re-read proves *server* persistence (AC4 tightening) + one edge. Confirm `ruff`/`mypy --strict`/coverage gates + the gear-modal preferences tests stay green (AC6, D5).
- [x] **T5 — Docs + stale-comment correction + tracked follow-up** (D4) — correct `config/__init__.py:17-19` (`api_key` stays for the `POST /api/v1/events` service-token per ADR-23/11-1 D1 — drop "Removed when E11-S3 lands") and `preferences.py:5-9` (the re-key it promised is now done — point at 0011). **Add a tracked follow-up** (one line in `deferred-work.md` and/or an ADR-23 note): shared static `api_key` → per-producer service identity is Phase-2 (D4 rider, Mary — so a security reviewer sees "known/scoped/tracked"). One-line note in `cloud-backend/CLAUDE.md` recording that `operator_preferences` is now FK'd to `users` (0011) + the drop/seed re-key Decision. **ADR FRESHNESS:** no ADR is contradicted (ADR-23 already anticipated the FK — [0009 docstring:12](../../cloud-backend/migrations/versions/0009_users.py) says *"from E11-S3, the operator_preferences FK"*); confirm no ADR edit needed and record that confirmation.

## Dev Notes

### Architecture patterns & constraints

- **ADR-8:** every route under `/api/v1/`. **ADR-10:** error envelope `{"error","detail","recoverable"}` for 401/422. **ADR-23 (11-1):** the verify seam — `_verify_token` stays pure; the liveness check lives in the extractor. This story adds no auth logic; it inherits both extractors unchanged (preferences is already behind `Security(get_current_user)`).
- **cloud-backend/CLAUDE.md "Migration safety":** every Alembic migration must be safe under concurrent reads — no full-table lock on `events`/`alerts`. 0011 only locks the empty `operator_preferences` table (D3) — compliant.
- **cloud-backend/CLAUDE.md "Review Failure Scenarios":** the migration must be safe; the preferences path must not 500 on a missing row (it 404s → client defaults — preserve that, it's the AC).

### Files being modified (UPDATE) — current state / change / preserve

Per the FULL-FILE-READS rule, each was read completely. Verified current state:

- **[routes/preferences.py](../../cloud-backend/src/cloud_backend/routes/preferences.py)** (146 lines) — *current:* GET (404→client-defaults) + PATCH (upsert) BOTH already key by `current_user.user_id` and bind `:oid`; router is `Security(get_current_user)`; `PreferencesOut`/`PreferencesPatch` have only `threshold_sec` + `staleness_threshold_sec`. *change:* essentially NONE to logic (11-1 already swapped identity) — verify-only, plus fix the stale docstring (T5). *preserve:* the 404→defaults contract (it's an AC of E2-S8 and the frontend relies on it — `api/preferences.js:34` maps 404→defaults), the `:oid` binding, the two threshold validators.
- **[config/__init__.py](../../cloud-backend/src/cloud_backend/config/__init__.py)** (60 lines) — *current:* `api_key` field with a comment claiming E11-S3 removes it. *change:* correct the comment only (D4/T5) — `api_key` STAYS (ingest service-token). *preserve:* the field itself, `require_api_key` importability, the bcrypt floor validator.
- **[migrations/versions/0002_operator_preferences.py](../../cloud-backend/migrations/versions/0002_operator_preferences.py)** — *reference only* (the table shape 0011 re-keys; do NOT edit a shipped migration). 0011 is a NEW file.
- **[control-centre/src/components/shell/OperatorPreferences.jsx](../../control-centre/src/components/shell/OperatorPreferences.jsx)** (188 lines) — *current:* gear-modal with `SegmentedControl` + 3 preference rows wired to `useFleetData()`; focus-trap + Escape-close on the modal. *change:* extract the shared control + preference-row body so Profile reuses it (D5). *preserve:* the modal's backdrop/focus-trap/Escape behavior (keep on the modal); the existing `useFleetData()` wiring; the keyboard-nav semantics of `SegmentedControl`.
- **[control-centre/src/App.jsx](../../control-centre/src/App.jsx)** (55 lines) — *current:* `RequireAuth` + `RequireAdmin` guards; `/dashboard/*` child routes incl. admin-gated `users`. *change:* add a `/dashboard/profile` route behind `RequireAuth` (NOT `RequireAdmin` — every user has a profile). *preserve:* the existing guard/redirect behavior and route order.
- **[control-centre/src/components/shell/AppShell.jsx](../../control-centre/src/components/shell/AppShell.jsx)** — *current:* nav tabs + admin-gated Users tab + the gear button that opens the prefs modal. *change:* add a Profile nav entry (all authenticated users). *preserve:* the gear-button modal trigger (D5 — keep both) and the admin-gating of Users.

### Re-key migration shape (D3 — the load-bearing piece)

```
# 0011_preferences_rekey_user_id.py  (down_revision = "0010")
def upgrade():
    op.execute("DELETE FROM operator_preferences")            # D1 — defunct API-key-keyed rows
    op.alter_column("operator_preferences", "operator_id",
        type_=postgresql.UUID(as_uuid=False),
        postgresql_using="operator_id::uuid")                 # safe: table empty
    op.create_foreign_key(
        "fk_operator_preferences_user", "operator_preferences",
        "users", ["operator_id"], ["user_id"], ondelete="CASCADE")
def downgrade():
    op.drop_constraint("fk_operator_preferences_user", "operator_preferences", type_="foreignkey")
    op.alter_column("operator_preferences", "operator_id",
        type_=sa.Text(), postgresql_using="operator_id::text")
```

Mirror 11-2's 0010 migration test (real downgrade→upgrade re-apply + schema asserts). The `users.user_id` referenced column is `UUID(as_uuid=False)` ([0009:29](../../cloud-backend/migrations/versions/0009_users.py)) — match it exactly so the FK types align.

### CSS tokens (Profile.jsx)

Use only `--obb-*` from [control-centre/src/styles/colors_and_type.css](../../control-centre/src/styles/colors_and_type.css). Verified-present tokens: surfaces `--obb-surface-0..5`; text `--obb-text-on-dark-1..4`; borders `--obb-border-dark`/`--obb-border-bright`; accent `--obb-blue-accent`. Severity ramp (if a save-error/confirm cue is needed): `--obb-sev-critical` (red, #FF3B3B), `--obb-sev-high` (orange), `--obb-sev-medium` (amber), `--obb-sev-advisory` (blue), `--obb-sev-normal` (green). **`--obb-sev-warning` does NOT exist (use `--obb-sev-medium`); `--obb-sev-danger` does NOT exist (use `--obb-sev-critical`).** Reuse `OperatorPreferences.css` patterns for the extracted control rather than inventing new token usage.

### UX ownership (Freya)

The Profile screen is a **new UX surface**. Per the epic's UX-pass plan ([epics.md:2558](../../_bmad-output/planning-artifacts/epics.md)), each E11 story ships a **minimal functional** UI (theme-correct, browser-verified); Freya runs ONE coherent WDS pass over the whole admin/identity shell (login + Users + **Profile** + Alert-Classes + Configuration) at epic close. Do NOT over-design Profile in isolation now — username/role header + the two reused preference controls satisfies AC4; Freya reviews in the browser-verify step.

### Testing standards

- Markers: `@pytest.mark.unit` (no DB) / `@pytest.mark.integration` (testcontainers Postgres). Coverage ≥80%; the isolation + FK paths near-complete.
- `mypy --strict` + `ruff` clean on touched `src/`.
- CC: `vitest` for `src/components/profile/Profile.jsx` states + the extracted control; the existing `OperatorPreferences` tests must stay green (D5 extraction must not regress the modal).
- Mirror 11-1/11-2's strengthened migration test (real downgrade→upgrade, schema assertions) for 0011.

### Previous-story intelligence (11-1, 11-2)

- **11-1** shipped the JWT seam + cut `preferences.py` over to `current_user.user_id` (so this story's "identity swap" is done) and carved `POST /api/v1/events` back to `require_api_key` (D4 — `api_key` survives).
- **11-2** shipped `0010_user_audit`, the liveness extractors, and `tests/integration/conftest.py::seed_auth_users` + `auth_header()` with fixed-UUID synthetic users — **reuse these for the isolation test's two real users** (or create two fresh users via `create_user` per A3; the fixed synthetic UUIDs are operator/admin — for a two-*operator* isolation test, create two real users via the real path so both satisfy the new FK). The liveness check means any token's `sub` must exist + be active in `users`; the new FK reinforces that for preferences.
- **11-2 lesson:** adding a DB dependency rippled across many tests. This story adds **no new auth dependency** (preferences already depends on `get_current_user`/`get_db`), so no comparable ripple — but the **new FK** means any integration test that raw-inserts an `operator_preferences` row with an unknown `operator_id` will now fail. That's intended (the FK is the point); fix such tests to seed a real user first (A3).

### Project Structure Notes

- Backend: `migrations/versions/0011_*.py` (new), `routes/preferences.py` (verify + comment fix only), `config/__init__.py` (comment fix) — matches cloud-backend/CLAUDE.md.
- Frontend: `src/components/profile/Profile.jsx` + `.css` — `profile/` is a NEW component dir (current dirs: admin, alerts, analytics, auth, escalations, health, live, luggage, occupancy, shell, train-detail). Follows `src/components/<feature>/`. The extracted control lands under `shell/` (alongside `OperatorPreferences.jsx`) or a small shared location — keep it next to its current home to minimise churn.

### References

- [Source: epics.md#Epic-11 → E11-S3](../../_bmad-output/planning-artifacts/epics.md) — locked scope/ACs/deliverables (note 0010→0011 + identity-swap-already-done divergences, D1/D2).
- [Source: 11-1-jwt-auth-foundation-login.md](11-1-jwt-auth-foundation-login.md) — JWT foundation; the preferences identity-swap + the ingest `require_api_key` carve-back (D4).
- [Source: 11-2-user-management.md](11-2-user-management.md) — `create_user` (A3 seed), `seed_auth_users`/`auth_header` integration fixtures, the 0010 migration-test shape to mirror.
- [Source: routes/preferences.py](../../cloud-backend/src/cloud_backend/routes/preferences.py) — already keyed by `current_user.user_id`; the 404→defaults contract to preserve.
- [Source: migrations/versions/0002_operator_preferences.py](../../cloud-backend/migrations/versions/0002_operator_preferences.py) — the TEXT-keyed table 0011 re-keys.
- [Source: migrations/versions/0009_users.py](../../cloud-backend/migrations/versions/0009_users.py) — `users.user_id` (UUID, FK target); docstring already names the E11-S3 FK.
- [Source: control-centre/src/components/shell/OperatorPreferences.jsx](../../control-centre/src/components/shell/OperatorPreferences.jsx) — the controls Profile reuses (D5).
- [Source: control-centre/src/context/FleetContext.jsx](../../control-centre/src/context/FleetContext.jsx) — the prefs GET/PATCH wiring; the localStorage-then-GET init (AC4 tightening — clear localStorage to prove server persistence); the `unattended_threshold_min` localStorage-only gap (D5 — kept off Profile).
- [Source: cloud-backend/migrations/env.py](../../cloud-backend/migrations/env.py) — confirms migrations run inside one transaction (`connection.begin()`) → 0011 rolls back atomically (D3).
- [Source: cloud-backend/src/cloud_backend/routes/ingest.py](../../cloud-backend/src/cloud_backend/routes/ingest.py) — `require_api_key` still live on `POST /api/v1/events` (D4 — api_key stays).

### Permission tier & review tier

- **Permission tier: Tier 3** — DB migration on EXISTING data (`operator_preferences`). **Default permission mode** (project CLAUDE.md: Tier 3 on shared infra — migrations — always default mode regardless of session mode). **Sign-off required in-story before T1.**
- **Review tier: FULL adversarial wire-replay** (A2 — migration on existing data). Code-review must replay the real two-user isolation wire (real `create_user` → real Bearer → cross-user PATCH/GET, NOT synthetic kwargs), adversarially probe the body-supplied-`operator_id` bypass (Security Test 1), and verify the 0011 migration on a DB seeded with a defunct non-UUID row (AC3) — not just an empty-table apply.

## Per-story Failure Scenarios (system-understanding required)

1. **Cross-user preference write via a forged body field (AC1/Security Test 1):** the endpoint takes identity from the token, but a careless refactor that re-introduces an `operator_id` body field (or trusts a header) would let operator A overwrite operator B's threshold by sending B's `user_id`. The review must confirm identity flows **only** from `current_user.user_id` into `:oid`, with no client-supplied path — replaying the real two-user wire, not a single-user happy path.
2. **Re-key migration string→uuid cast failure on real defunct data (AC3/D1):** the table today holds rows keyed by the API-key string (a non-UUID TEXT). If the migration ALTERs the column type *before* deleting those rows, `operator_id::uuid` throws (`invalid input syntax for type uuid`) and the migration aborts. The whole migration is one transaction (✅ Winston-verified via env.py), so it rolls back atomically — there is **no** half-migrated state — but a rolled-back migration is still a *failed* migration that blocks the deploy. The DELETE must precede the ALTER (D3); the test must seed a **genuinely non-UUID-castable** row (e.g. `'shared-api-key-abc'`, not a random UUID-string) and prove the upgrade succeeds, not run against an empty table (or a UUID-string row) that hides the ordering bug.

## Open Questions — ✅ ALL RESOLVED (party-mode 2026-06-15)

1. **D3 migration shape:** ✅ **in-place `DELETE + ALTER + ADD FK`** (Winston: atomicity verified, `USING` clause required, in-place preserves CHECKs for free; drop-and-recreate rejected as more surface).
2. **D5 modal vs route:** ✅ **keep BOTH** the gear-modal (quick-access overlay) AND the Profile route (canonical home) — Freya: a normal shell pattern, not confusing duplication; unification deferred to the epic-close WDS pass.
3. **D5 `unattended_threshold_min`:** ✅ **OFF the Profile screen** (Amelia's framing won the clash; Freya conceded) — stays on the gear-modal only until the backend column lands and it can graduate onto Profile as a real synced field. No server persistence this story.

> No residual blocker. Tier-3 sign-off before T1 still required (migration on existing data, default permission mode).

## Dev Agent Record

### Agent Model Used

claude-opus-4-8[1m] (Amelia)

### Debug Log References

- Browser-verify backend: disposable Postgres (docker `oebb-verify-pg` :5544) → `alembic upgrade head` (0011 applied) → seeded admin `claudia` via real `create_user` → uvicorn :8002 → Vite `control-centre-verify-8002` proxy. Torn down after verification.

### Completion Notes List

- **All 6 ACs satisfied.** AC1 two-user isolation (real `create_user` seed, A3), AC2 catalog-asserted UUID+FK+CHECK survival, AC3 migration drops genuinely-non-UUID-castable defunct rows + re-applies idempotently, AC4 browser-verified server persistence (localStorage cleared between change and reload), AC5 logged-out→401 / valid-token-no-row→404 carve-out, AC6 gates green + modal unregressed.
- **REAL BUG CAUGHT BY THE INTEGRATION GATE (the headline).** After 0011 retyped `operator_id` to `uuid`, the raw-SQL `RETURNING operator_id` returns a Python `UUID` object, but `PreferencesOut`/`PreferencesPatchOut.operator_id` are typed `str` → Pydantic `ValidationError` → **500 on every GET/PATCH**. A unit test (mocked `get_db` returning a str) would have stayed green; only the real-Postgres path surfaced it. Fixed by coercing `str(row.operator_id)` at both return sites (the model stays `str` per Winston's type-seam ruling — the column is `uuid`, the contract is `str`). This is exactly the test/prod-divergence class the E10-A1 gate exists to catch.
- **D-decisions implemented as locked.** D1 DROP made legible (a `log.warning` emits the deleted row count; the migration carries the why-it's-safe comment + CASCADE-rationale + downgrade-restores-schema-not-rows docstring). D2 migration is 0011 (`down_revision="0010"`). D3 in-place DELETE→ALTER(`USING ::uuid`)→ADD-FK(CASCADE), DELETE strictly first; whole migration is one transaction (atomic). D4 `api_key` kept (still live on ingest), both stale comments fixed, Phase-2 service-identity follow-up tracked in deferred-work.md. D5 `SegmentedControl` extracted to a shared file (modal + Profile import it; modal tests stay green — zero focus-trap change); Profile ships only the two server-backed controls; `unattended_threshold_min` stays modal-only (tracked E11S3-D5).
- **AC tightenings honoured.** AC2 asserts the two DB CHECKs via `pg_constraint` catalog (not an app-layer 422). AC4 browser-verify cleared localStorage between change and reload so the re-read proves *server* persistence (DB showed 60 after the UI click; cleared localStorage; reload showed 60s active from the server GET). AC5 added the valid-token-no-row→404 carve-out test.
- **GATES (all green):** backend **273 passed** (unit + integration on real Postgres via testcontainers — A1 gate ran locally, Docker up); **mypy --strict** clean (40 files); **ruff** clean (src + tests); coverage **TOTAL 82.46%** (≥80 gate), `routes/preferences.py` **100%**. Frontend **281 vitest** passed (275 baseline + 6 new). Migration `0011` re-applies idempotently (downgrade 0010 → upgrade head).
- **Browser-verified (AC4):** against the real seeded backend — (1) unauthenticated `/dashboard/profile` → redirect to /login (AC5); (2) golden — login as `claudia` → Profile renders username + ADMIN pill + the two server-backed controls (90s active = the server value); change to 60s → DB shows 60 → **clear localStorage** → reload → 60s active *from the server* (true server persistence, not stale cache); (3) only the two controls present — no unattended-bag control (D5). Console clean except the pre-existing `fetchTrainAlerts` mock-train 404s (404 not 401 = auth flowing; documented baseline, not introduced here).
- **eslint:** the repo baseline already carries 37 eslint errors (`refs-during-render`, `only-export-components` fire broadly — not a clean gate here). Net change from this story is **zero new avoidable violations**: the one `refs-during-render` on `SegmentedControl.jsx` is pre-existing code relocated verbatim from `OperatorPreferences.jsx` (confirmed identical at baseline); the `only-export-components` I briefly introduced was eliminated by moving `formatSec` into `constants/preferences.js`. `AuthContext.jsx only-export-components` is the same pre-existing item 11-2 documented.

### File List

**Backend (new):** `cloud-backend/migrations/versions/0011_preferences_rekey_user_id.py`, `cloud-backend/tests/integration/test_preferences_rekey.py`
**Backend (modified):** `cloud-backend/src/cloud_backend/routes/preferences.py` (UUID→str coerce at both return sites + docstring fix), `cloud-backend/src/cloud_backend/config/__init__.py` (api_key stale-comment fix — D4), `cloud-backend/tests/integration/test_migrations.py` (operator_preferences UUID+FK+CHECK catalog asserts), `cloud-backend/tests/unit/test_preferences_api.py` (AC5 valid-token-no-row→404 carve-out test), `cloud-backend/CLAUDE.md` (preferences FK + UUID-coerce note)
**Frontend (new):** `control-centre/src/components/profile/Profile.jsx`, `control-centre/src/components/profile/Profile.css`, `control-centre/src/components/profile/__tests__/Profile.test.jsx`, `control-centre/src/components/shell/SegmentedControl.jsx`, `control-centre/src/components/shell/__tests__/SegmentedControl.test.jsx`
**Frontend (modified):** `control-centre/src/components/shell/OperatorPreferences.jsx` (import extracted SegmentedControl — D5), `control-centre/src/constants/preferences.js` (formatSec home), `control-centre/src/lib/auth/tokenStore.js` (getUsername), `control-centre/src/context/AuthContext.jsx` (expose username), `control-centre/src/App.jsx` (/dashboard/profile route), `control-centre/src/components/shell/AppShell.jsx` (Profile nav)
**Docs:** `_bmad-output/implementation-artifacts/deferred-work.md` (E11S3-D4 service-identity + E11S3-D5 unattended follow-ups)

## Change Log

| # | Change | Rationale |
|---|---|---|
| 1 | create-story grounded the epic against 11-1's shipped reality | Epic was authored pre-11-1. Surfaced 5 Decisions: D1 drop/seed re-key (epic-endorsed); D2 migration is 0011 not 0010 (0010=user_audit, shipped by 11-2); D3 in-place DELETE+ALTER+ADD-FK shape (DELETE before ALTER so string→uuid cast can't fail); D4 `api_key`/`require_api_key` STAYS (11-1 carved ingest back to it — `config:19` "Removed when E11-S3 lands" is stale); D5 Profile reuses the existing gear-modal controls (no rebuild) + `unattended_threshold_min` is localStorage-only and out of scope. Identity-source swap already done by 11-1 → this story is migration + FK + Profile screen, materially lighter than the epic implied. |
| 2 | Party-mode (Winston/Amelia/Mary/Freya) resolved all 5 Decisions + 1 clash + 3 AC tightenings | **D1** DROP confirmed + made LEGIBLE (why-safe comment, `log.warning` deleted-count, downgrade-restores-schema-not-rows docstring — Mary). **D3** in-place shape confirmed; Winston verified one-transaction atomicity (env.py), corrected "`USING` is pointless"→required, added CASCADE-rationale doc + PK-survives assertion + the `str`-not-`UUID` response-model type-seam note; Amelia confirmed grep shows NO existing test touches `operator_preferences` (FK breaks nothing) + AC3 seed must be non-UUID-castable. **D4** api_key stays + tracked Phase-2 service-identity follow-up (Mary). **D5 CLASH:** extract `SegmentedControl` (Amelia: already decoupled, zero focus-trap risk); keep BOTH modal+route (Freya: shell pattern); `unattended_threshold_min` OFF Profile (Amelia's "don't carry a persistence lie onto the persistence screen" — Freya conceded her modal-parity-with-cue position). **AC tightenings (Amelia):** AC4 clear localStorage between logout/login (FleetContext inits from localStorage → stale-cache false-pass); AC5 add valid-token-no-row→404 carve-out; AC2 assert DB CHECKs via `pg_constraint` catalog, not an app-layer 422. |
| 3 | dev-story implementation (Amelia) — all 6 ACs, gates green, browser-verified | 0011 migration (DELETE→ALTER→FK, legible deleted-count log); `preferences.py` UUID→str coerce (the real 500 bug the integration gate caught — uuid column vs str model); `SegmentedControl` extracted to a shared file + `formatSec`→`constants/preferences.js` (modal unregressed); new Profile route (`getUsername` added to tokenStore/AuthContext, two server-backed controls, unattended deliberately omitted); D4 stale-comment fixes + tracked follow-ups. 273 backend (real PG) / 281 frontend / mypy --strict / ruff all green; coverage 82.46% (preferences.py 100%). Browser-verified server persistence by clearing localStorage between change and reload. |
