# generated: 2026-05-17
# last_updated: 2026-06-15  # code-review (FULL adversarial wire-replay) + security-sentinel: 11-3-profile-management-prefs-rekey → DONE. Three review layers; the Edge Case Hunter stood up a real Postgres and EMPIRICALLY replayed the 0011 migration on realistic pre-migration data (proved DELETE-before-ALTER is required: `ALTER ::uuid` on 'dev-insecure-key' raises 22P02 invalid_text_representation; the uuid-lookalike orphan edge is cleared by the unconditional DELETE before the FK is added; downgrade→upgrade re-apply; ON DELETE CASCADE) and the live login→PATCH→GET wire with zero 500s — and resolved 6 of the Blind Hunter's out-of-diff guesses as false (JWT carries username; FleetContext.updateAlertThreshold resolves-with-Error so the toast works; parent /dashboard enforces RequireAuth). NO behaviour violation. 1 decision (Profile avatar vs D5 "no avatar")→KEEP (user; Freya owns identity treatment at epic-close). 3 mandated RED-first test patches added (junk-sub SQL-safety with canary-row + to_regclass survival; expired+tampered tokens 401 on GET AND PATCH; caplog/Logger-spy deleted-count assertion immune to alembic's fileConfig disable_existing_loggers). The Security-Test-2 patch SURFACED A REAL LATENT BUG (pre-existing E11-S2, not 11-3): a non-UUID `sub` 500'd at assert_user_active's `WHERE user_id=:uid` bind (uuid column rejects it) instead of 401 — FIXED via try/except DBAPIError→401 (also removes a 500-stack-trace info-leak; sentinel: this IMPROVES posture). First fix (pre-validate UUID) regressed 7 unit tests minting sentinel subs vs mocked DBs → caught by the full-suite gate → re-fixed by catching the bind error (mocked tests never raise it). Also fixed a module-shared-fixture row-leak that made the deleted-count assertion order-dependent (DELETE before the single-row seed). POST-PATCH GATES GREEN: backend 275 passed (real PG, A1 gate, deterministic ×2), mypy --strict clean (40), ruff clean, cov 82.37% (preferences.py 100%); frontend 281 vitest. SECURITY-SENTINEL APPROVED (no Critical/Major; authz/secret-hygiene/injection/log-safety/frontend PASS; video/Hailo/Conrad/SSE N/A; 2 Observations = pre-existing already-tracked api_key + unattended follow-ups). Epic 11: 3 of 5 done (S1,S2,S3); S4 unblocked. Commits: dev 640624b + review patches pending.
# last_updated: 2026-06-15  # dev-story (Amelia): 11-3-profile-management-prefs-rekey → review. All 6 ACs implemented; baseline a7e6a2d. Alembic 0011 re-keys operator_preferences.operator_id from the retired shared-API-key TEXT string to a UUID FK on users(user_id): DELETE defunct rows (+ log.warning count, D1 legibility) → ALTER COLUMN TYPE uuid USING ::uuid → ADD FK ON DELETE CASCADE, all in one transaction (atomic). HEADLINE — the A1 integration gate caught a REAL test/prod-divergence 500: post-0011 the raw-SQL `RETURNING operator_id` returns a Python UUID object but PreferencesOut/PreferencesPatchOut.operator_id are str-typed → ValidationError → 500 on every GET/PATCH; a mocked unit test (get_db returns a str) would have stayed green. Fixed with str() coerce at both return sites (model stays str — Winston's type-seam ruling; column is uuid). Frontend: extracted SegmentedControl to a shared file (modal unregressed, zero focus-trap change), moved formatSec to constants/preferences.js, added getUsername to tokenStore + exposed username via AuthContext, new /dashboard/profile route behind RequireAuth (username + ADMIN pill + the 2 server-backed controls; unattended_threshold_min deliberately OMITTED — D5 clash-resolution, tracked E11S3-D5). D4: api_key/require_api_key kept (live on ingest), both stale "Removed when E11-S3 lands" comments fixed, Phase-2 per-producer service-identity tracked (E11S3-D4). GATES GREEN: backend 273 passed (unit + integration on real Postgres via testcontainers — A1 hard gate ran locally, Docker up), mypy --strict clean (40 files), ruff clean, coverage 82.46% (preferences.py 100%); frontend 281 vitest (275 baseline + 6 new). BROWSER-VERIFIED (AC4 server persistence): change alert threshold to 60 in the UI → DB shows 60 → CLEAR localStorage → reload → 60s active from the server GET (proves server persistence not stale cache); unauthenticated /dashboard/profile → /login (AC5); only the 2 server-backed controls render. Console clean except pre-existing mock-train fetchTrainAlerts 404s (404 not 401 = auth flowing). eslint net-zero new violations. Next: code-review (FULL adversarial wire-replay — migration on existing data) + security-sentinel (different LLM).
# last_updated: 2026-06-15  # party-mode (Winston/Amelia/Mary/Freya) resolved 11-3 Decisions. All 5 locked + 1 clash settled + 3 AC tightenings, story still ready-for-dev (richer). D1 DROP confirmed but made LEGIBLE (why-safe comment + log.warning deleted-count + downgrade-restores-schema-not-rows docstring — Mary). D3 in-place DELETE→ALTER→ADD-FK confirmed; Winston VERIFIED one-transaction atomicity via migrations/env.py (connection.begin → atomic rollback, no half-migrated state), corrected "USING ::uuid is pointless on empty table"→it's REQUIRED (Postgres has no implicit text→uuid cast in ALTER COLUMN TYPE), added CASCADE-rationale docstring + PK-survives-ALTER assertion + the str-not-UUID response-model type-seam note (as_uuid=False returns str). Amelia: grep of tests/ for operator_preferences = ZERO hits → new FK breaks nothing today; AC3 migration seed MUST be a genuinely non-UUID-castable TEXT value (e.g. 'shared-api-key-abc') not a random UUID-string, else an ALTER-before-DELETE bug passes undetected; isolation test seeds TWO FRESH operators via real create_user (NOT the conftest operator/admin synthetics — they're raw-psycopg2 password_hash='x', skip the real-bcrypt A3 path). D4 api_key STAYS (live on ingest.py:29 per ADR-23) + tracked Phase-2 service-identity follow-up so a security reviewer sees known/scoped (Mary). D5 CLASH RESOLVED: extract SegmentedControl (Amelia — it's already fully decoupled from the modal focus-trap, literal cut-paste, render-inline is the more awkward option); keep BOTH gear-modal + /dashboard/profile route (Freya — quick-access overlay vs canonical home, normal shell pattern, unification deferred to epic-close WDS pass); unattended_threshold_min OFF the Profile screen (Amelia's "don't carry a persistence lie onto the persistence screen whose whole thesis is settings-follow-you" — Freya CONCEDED her modal-parity-with-"saved-on-this-device"-cue position), stays gear-modal-only until backend column lands. AC tightenings (Amelia, all would otherwise bounce review): AC4 browser-verify MUST clear localStorage between logout/login (FleetContext inits from localStorage then GET-reconciles → stale cache can false-pass a broken server write); AC5 add explicit valid-token-no-row→404-defaults carve-out (preserved E2-S8 contract the frontend depends on) so it doesn't read as contradicting Security Test 3; AC2 assert the two DB CHECKs via pg_constraint/information_schema catalog, NOT via an app-layer 422 that never reaches the DB. NOT yet dev'd.
# last_updated: 2026-06-15  # create-story (Amelia): 11-3-profile-management-prefs-rekey → ready-for-dev. Epic-11 S3 (S3∥S4 unblocked, S1+S2 done). GROUNDED against 11-1's shipped reality — 5 Decisions surfaced because the epic was authored pre-11-1: D1 drop/seed re-key (epic-endorsed: defunct rows keyed by the dead API-key string, no honest 1-key→N-user mapping → DELETE not migrate); D2 migration is 0011 NOT 0010 (0010=user_audit, already shipped by 11-2 — epic numbering stale); D3 in-place DELETE+ALTER COLUMN TYPE UUID+ADD FK→users(user_id) ON DELETE CASCADE, DELETE BEFORE ALTER so the string→uuid cast on real defunct data can't fail mid-flight; D4 api_key/require_api_key STAYS (11-1 R1 carved POST /api/v1/events back onto require_api_key as the documented machine-to-machine exception — config:19 "Removed when E11-S3 lands" is stale → comment-fix only, not removal); D5 Profile screen REUSES the existing gear-modal SegmentedControl (extract shared body, no rebuild) + unattended_threshold_min is localStorage-only today (backend never persisted it) → OUT of scope. KEY FINDING: 11-1 ALREADY swapped preferences.py to key by current_user.user_id AND the frontend already PATCHes via Bearer — so the epic's "identity swap" is DONE; residual work = schema re-key migration (0011) + FK + Profile route. Materially lighter than the epic implied. 6 ACs (AC1 two-user isolation is the core guarantee; AC3 migration drops defunct non-UUID rows; AC5 logged-out→401). 3 RED-first security tests (no client-supplied operator_id; SQL-safety on uuid key; logged-out 401). A1 integration gate: test_preferences_isolation_two_users seeds 2 real users via create_user (A3) + FK-rejects-unknown-user negative test + rekey-drops-defunct-rows + 0010→0011 re-apply. Tier 3 (migration on existing data), default mode, FULL adversarial wire-replay review, browser-verify required. Depends: 11-1+11-2 (both done). 3 non-blocking pre-flight confirms (migration shape, keep-modal-vs-route, unattended scope). NOT yet dev'd.
# last_updated: 2026-06-15  # code-review (FULL adversarial wire-replay) + security-sentinel: 11-2-user-management → done. Review found 5 patches (all test-coverage/edge gaps, no behaviour violation), 2 defers, 4 dismissed incl. a refuted "Blocker" (bcrypt floor was actually correct). All 5 patches applied+test-locked: concurrent-create 409-not-500 (IntegrityError catch), password byte-cap (max_length was chars not bytes → multibyte truncation footgun), empty/junk-role-on-admin-route test (D7 was unguarded), tightened concurrent-guard assertion, APP_ENV-unset floor test. Bonus: fixed unit test_tampered_signature flaky (last-char base64 flip ~5.5% no-op, same species as the integration one). Gates: 265 backend passed (real-PG, deterministic), mypy --strict + ruff clean, users vitest 11/11. Sentinel APPROVED (no Critical/Major). 2 deferred to deferred-work.md (in-flight SSE revocation; frontend 403 handling — both epic-wide/pre-existing). Epic 11: 2 of 5 stories done (S1, S2); next S3∥S4.
# last_updated: 2026-06-14  # dev-story (Amelia): 11-2-user-management → review. Implemented T1-T7: api/users.py models (12-72 pwd policy), bcrypt_rounds Setting + fail-closed <10-unless-test validator, Alembic 0010 user_audit, is_active liveness check in BOTH extractors (kept _verify_token pure → ADR-23 seam intact), routes/users.py admin CRUD with race-safe FOR UPDATE last-admin guard, in-transaction audit writes, frontend Users screen (role-gated nav + RequireAdmin route). Gates: backend 257 passed (real-PG integration A1 gate ran locally, deterministic 3x), mypy --strict clean, ruff clean, cov 83%; frontend 275 vitest. Browser-verified 4 paths (redirect/golden/last-admin-409/operator-blocked). Caught+fixed: last-admin UUID-vs-str guard bug, latent 11-1 tamper-test flaky (last-char base64 flip ~5.5% no-op → reverse sig), cross-module JWT_SECRET leak. Liveness ripple fixed centrally (unit extractor-bypass + integration seed_auth_users). FULL adversarial review next.
# last_updated: 2026-06-14  # party-mode (Winston/Amelia/Mary) resolved 11-2 pre-flight decisions: D1→Option A (dedicated user_audit table + Alembic 0010, NO shared/ change); D6→ADD bcrypt_rounds knob as a TEST-SPEED lever (prod stays 12) guarded by fail-closed Settings validator (reject <10 unless app_env=test); D3 password policy→NIST length-only min_length=12, no composition. 3 hardening riders added: 72-byte max_length cap (AC1), fresh-user liveness pin (AC3, closes AC1↔D2 contradiction), seam-purity guard test (D2/T2 — is_active stays in extractor not _verify_token, protects 11-1 AC4/ADR-23). All open questions closed; one non-blocking pre-flight confirm (ÖBB corporate pwd standard if it exists). Story still ready-for-dev. NOT yet dev'd.
# last_updated: 2026-06-14  # create-story (Amelia): 11-2-user-management → ready-for-dev. Builds on 11-1 (DONE): reuses get_current_user/require_role/create_user + users table (0009). Story surfaces 3 load-bearing decisions: D1 audit transport (RECOMMEND Option A = dedicated user_audit table + Alembic 0010, NO shared/ EventType+payload change — there is no USER_* EventType today, Option B would cascade to event-store + contract tests); D2 mid-session deactivation needs an is_active liveness check layered in BOTH extractors (header + SSE ?token=) AFTER _verify_token so 11-1's AC4 OIDC seam stays pure — this clears 11-1's D7 deferral; D3 last-active-admin lock-out guard must be race-safe (TOCTOU under concurrent deactivation = zero admins = permanent lockout). Also clears 11-1 Round-1 empty-role Defer (D7). Tier 3, FULL review, browser-verify required. 3 open questions for pre-flight (audit transport, bcrypt knob, pwd min-length). NOT yet dev'd.
# last_updated: 2026-05-30  # sprint-planning re-run: Hardening epics 6/7/8/9 added (all stories backlog, ACs in epics.md); E1-S6' added (cloud-backend SSE fan-out per ADR-20). Existing statuses preserved. Epic 1 retro-reopened from done → in-progress because E1-S6' is a new in-flight story under it.
# last_updated: 2026-05-30  # E10-S1 scoped via grill-me + party-mode: renamed to 10-1-alert-confidence-and-ai-pipeline-health (code-only); playbook content moved to E10-S3; new E10-S5 (alert quality measurement) + Epic 11 (CC Admin & Identity) added; full ACs at _bmad-output/implementation-artifacts/10-1-alert-confidence-and-ai-pipeline-health.md
# last_updated: 2026-05-30  # epic-10 added (Operator Adoption & Trust): 4 stories backlog (S1 confidence+exec playbook, S2 behavioural telemetry, S3 SOP+drill cadence, S4 dwell-time KPI) — source: owning-the-gap-ai-pm-analysis.md
# last_updated: 2026-05-21  # code-review 2026-05-21 (opus-4.7): 1-5-4-onboard-smoke-test → done; 7 patches applied (curl body, GET filter, cameras.json stub, CWD guard, compose v2, API_KEY env, exec bit); smoke test exits 0
# last_updated: 2026-05-21  # dev-story 2026-05-21: 1-5-4-onboard-smoke-test → review (smoke-test-onboard.sh exits 0; event-store POST+GET verified)
# last_updated: 2026-05-21  # dev-story 2026-05-21 (opus-4.7): 4-10-coach-comfort-index → review (139 fusion tests, cov 94.03%, mypy/ruff clean)
# last_updated: 2026-05-21  # dev-story 2026-05-21: 4-10-coach-comfort-index → in-progress
# last_updated: 2026-05-21  # code-review 2026-05-21 (opus-4.7): 4-9 → done; 17 patches applied + 5 decisions resolved; 121 fusion tests cov 94.15%
# last_updated: 2026-05-21  # dev-story 2026-05-21 (Amelia/opus-4.7): 4-9-closed-ledger-reconciliation → review (119 fusion tests, cov 94.66%, mypy/ruff clean)
# last_updated: 2026-05-21  # epic-4 phase-1 retrospective complete: 9 of 11 stories shipped; 8 action items A1-A8; phase-2 retro deferred until 4-9 + 4-10 done
# last_updated: 2026-05-21  # create-story 2026-05-21: 4-10-coach-comfort-index → ready-for-dev (depends on 4-9; no party-mode needed — follows 4-9 precedents)
# last_updated: 2026-05-21  # party-mode 2026-05-21: 4-9 D1-D5 resolved; LEDGER_DRIFT_ALERT renamed to LEDGER_DRIFT_OBSERVATION; aggregator dropped from scope
# last_updated: 2026-05-21  # create-story 2026-05-21: 4-9-closed-ledger-reconciliation → ready-for-dev (BLOCKED on D1-D4 checkpoint)
# last_updated: 2026-05-20  # 4-5-inference-safety-accessibility: review → in-progress (2 test failures + 7 decisions needed)
# project: oebb-agent
# project_key: NOKEY
# tracking_system: file-system
# story_location: _bmad-output/implementation-artifacts

# STATUS DEFINITIONS:
# ==================
# Epic Status:
#   - backlog: Epic not yet started
#   - in-progress: Epic actively being worked on
#   - done: All stories in epic completed
#
# Epic Status Transitions:
#   - backlog → in-progress: Automatically when first story is created (via create-story)
#   - in-progress → done: Manually when all stories reach 'done' status
#
# Story Status:
#   - backlog: Story only exists in epic file
#   - ready-for-dev: Story file created in stories folder
#   - in-progress: Developer actively working on implementation
#   - review: Ready for code review (via Dev's code-review workflow)
#   - done: Story completed
#
# Retrospective Status:
#   - optional: Can be completed but not required
#   - done: Retrospective has been completed
#
# WORKFLOW NOTES:
# ===============
# - Epic transitions to 'in-progress' automatically when first story is created
# - Stories can be worked in parallel if team capacity allows
# - Developer typically creates next story after previous one is 'done' to incorporate learnings
# - Dev moves story to 'review', then runs code-review (fresh context, different LLM recommended)

generated: 2026-05-17
last_updated: 2026-06-15  # create-story (Amelia): 11-4-admin-ui-alert-class-killswitch → ready-for-dev (Epic-11 S4; S1+S2+S3 done, S3∥S4). Auth swap on the LIVE E10-S1 kill-switch — SINGLE clean swap point (router dep admin_alert_classes.py:45 require_admin_key→require_role('admin'), mirrors users.py). ONE load-bearing Decision RESOLVED (user): actor=current_user.username (display-legible), AdminActionBody.actor_name REMOVED (no body-spoof). PAYLOAD AUDIT cleared (AlertClassStatePayload unchanged, only value source body→token; no shared/migration). ADR-23 already anticipates X-Admin-Key retirement (no ADR change). cc_admin_key REMOVED (zero readers post-swap; symmetric-opposite of 11-3 D4 api_key-stays). Frontend = clean Users mirror (NEW AlertClasses.jsx+api+test; modify App/AppShell; zero existing caller). 6 ACs (AC1 fan-out suppression preserved through new auth = core guarantee). A1 gate: real JWT toggle→ingest→assert suppressed. Tier 3, default mode, FULL review, browser-verify. Depends 11-1+11-2.
last_updated: 2026-06-15  # code-review (FULL adversarial wire-replay) + security-sentinel: 11-3-profile-management-prefs-rekey → DONE. Edge Case Hunter empirically replayed the 0011 migration on real PG (proved DELETE-before-ALTER, uuid-lookalike-orphan edge, CASCADE, re-apply) + the live auth→prefs wire; resolved 6 Blind-Hunter out-of-diff guesses as false. No behaviour violation. 1 decision (avatar)→KEEP. 3 mandated test patches added; Security-Test-2 surfaced + fixed a real latent bug (non-UUID sub → 500 not 401 at the liveness bind, pre-existing E11-S2 → try/except DBAPIError→401). 275 backend (real PG, deterministic ×2) / 281 frontend / mypy --strict / ruff green; cov 82.37%. Sentinel APPROVED (no Critical/Major). Epic 11: 3/5 done; S4 unblocked.
last_updated: 2026-06-15  # create-story (Amelia): 11-3-profile-management-prefs-rekey → ready-for-dev (Epic-11 S3; S1+S2 done). Grounded vs 11-1 shipped reality: identity-swap already done by 11-1 → residual = re-key migration 0011 (NOT 0010=user_audit) + FK to users + Profile screen. 5 Decisions (D1 drop/seed; D2 0011; D3 DELETE-before-ALTER cast-safety; D4 api_key stays for ingest; D5 reuse modal controls). Tier 3 migration, FULL review. Depends 11-1+11-2.
last_updated: 2026-06-14  # create-story (Amelia): 8-3-landside-sse-client-migration → ready-for-dev; epic-8 → in-progress (first story created). Fixes the pre-existing landside live-transport mismatch surfaced during 11-1 D8 planning: shipped frontend live client is RealWebSocketClient → new WebSocket(VITE_WS_URL)/`/ws`, but cloud-backend has NO /ws (only SSE /api/v1/alerts/stream per ADR-20). WS client is DEAD in prod (VITE_WS_URL always empty → only MockWebSocketClient runs). Root cause: story 2-1 shipped the WS client 2026-05-17 BEFORE ADR-20 (2026-05-30) rewrote E2-S1 to SSE; that rewrite (epics.md:517-551) was never built. This story implements it: new src/sse/RealSseClient.js (EventSource against /api/v1/alerts/stream, named-event listeners per ALERT_EVENT_TYPES allow-list, Last-Event-ID native reconnect, ported _handleEnvelope/dedup/normalisation D4), repoint FleetContext, DELETE RealWebSocketClient.js+test (D2 clean delete, NOT epics.md:547 stub — only FleetContext imports it), rename VITE_WS_URL→VITE_SSE_URL (D5). SEQUENCING (D3, user-confirmed): build now vs require_api_key gate; _buildUrl() isolated so 11-1 adds ?token= in one line (EventSource can't set Authorization header — 11-1 D8). ADR cross-ref freshness only (architecture.md:642 ❌→✅; no ADR decision change — ADR-20 already says SSE). 8 ACs, FULL review (cross-container SSE wire contract; D9 frame-shape WS≠SSE is the green-but-wrong trap). Tier 2 (no migration/shell/backend route). Browser-verify required. NOT yet dev'd.
last_updated: 2026-06-14  # create-story (Amelia): 11-1-jwt-auth-foundation-login → ready-for-dev; epic-11 → in-progress (first story). Self-contained JWT for PoC (local users+bcrypt, HS256), OIDC-swappable seam (AC4). Pre-flight verified: NO central auth middleware — cutover is 15 routers + 1 per-route /api/v1/health gate (capacity_review is TWO routers); infra probes /health/live+/ready stay open. 9 ACs (AC4 seam contract is the OIDC-swap guarantee; AC5 surface inventory; AC7 fail-closed). 8 RED-first security tests (alg:none, wrong-issuer, user-enumeration). Integration test seeds via REAL user-creation path (A3) + A1 hard gate. ADR-23 authored in architecture.md (NOT a separate file — D1 corrected the breakdown's guessed path); ADR-7/ADR-22 updated. D8 LOAD-BEARING: live /ws stream can't carry a Bearer header → token in the SUBSCRIPTION_REQUEST handshake (verify core shared, two extractors — preserves AC4 seam). Review tier FULL (auth+migration). Tier 3, default mode. Migration 0009. NOT yet dev'd.
last_updated: 2026-06-14  # 10-5 AC7 browser-verify DISCHARGED (Amelia) — and the "blocked" framing was a MISDIAGNOSIS: the SPA mounts fine in control-centre-mock (earlier empty-#root was a pushState+reload racing React's mount). NOTE: no VITE_MOCK_API flag exists in source (launch config sets one nothing reads); real mock seam is VITE_WS_URL-unset→MockWebSocketClient; the fetchTrainAlerts console errors are pre-existing, caught into state, don't crash the shell. Verified AIQualityRates live at /dashboard/health (rates endpoint stubbed): header + 2 columns (AC3) + 2-decimal % (n of d) + exactly 1 breach cell tinted rgb(245,166,35)=--obb-sev-medium + 14px text + clean console. Golden path browser-verified; edge states RTL-covered. 10-5 now has ZERO open items.
last_updated: 2026-06-14  # 10-5 integration residual DISCHARGED (Amelia): Docker came up → ran pytest -m integration on real Postgres. All 6/6 10-5 integration tests PASS (proves the FK _seed_parent BLOCKER fix + the novel ?-operator/jsonb_array_length SQL end-to-end); full cloud-backend integration suite 92 passed / 0 failed (no regressions). Only AC7 browser-verify remains open (pre-existing mock-config block, not a defect). 10-5 stays DONE, now with the SQL empirically proven not just structurally verified.
last_updated: 2026-06-14  # code-review R2 (Amelia, wf_0c944bdb): 10-5-alert-quality-measurement → DONE. All 3 R1 fixes re-verified correct end-to-end (FK _seed_parent satisfies every NOT-NULL parent col + matching eid, no residual constraint risk; untracked CC test now committed w/ 8 AC7 cases; 2-decimal label keeps % (n of d) + boundary test). 1 new low caught + fixed (fixture truncated only escalation_audit → leaked escalations parents → TRUNCATE both). APPROVED. Final epic-10 feature story shipped. Gates GREEN: CB unit 100 / CC vitest 258 / mypy --strict 37 files / ruff clean. Residual CI-gated: integration run (Docker) + browser-verify (pre-existing mock block) — same posture as 10-1/10-4. Commits ff96e4a + 9e280a3 + R2 fix.
last_updated: 2026-06-14  # code-review R1 (Amelia, multi-agent adversarial wf_4b011970, 14 agents): 10-5 → CHANGES_REQUESTED → fixed, stays review pending re-verify. Completeness critic caught the headline BLOCKER all 4 find-layers missed: integration tests violated escalation_audit→escalations FK (orphan escalation_id, no parent row) → all 4 data tests would ForeignKeyViolationError on first CI → novel `?`-operator SQL had ZERO passing coverage (green-test trap). FIXED: _seed_parent() inserts the escalations parent before every audit row. HIGH: AIQualityRates.test.jsx was untracked (never committed, absent from ff96e4a/CI despite "7 tests" claim) → now staged. MEDIUM `?`-operator verified correct via Postgres source (Context7: jsonb ? tests array-element membership; jsonb_array_length is proisstrict → NULL-safe). LOW: 2-decimal label (gate/tint consistent at 5% boundary) + RTL boundary test; _render NFR3 section tests added. 6 refuted. Accepted-deferral: browser-verify (pre-existing mock-config SPA-mount block). Deferred: --font-size-sm undefined token (pre-existing, 5 files). Gates GREEN post-fix: CB unit 100 / CC vitest 258 / mypy --strict 37 / ruff clean. Next: re-verify (run integration on Docker) then DONE.
last_updated: 2026-06-14  # dev-story (Amelia): 10-5-alert-quality-measurement → review. CODE-LIGHT/additive: new auth-guarded read-only /api/v1/ai-quality/resolution-rates (no_action_rate + explicit_fp_rate per alert_code off escalation_audit, 7-day skew-proof window) + AIQualityRates CC component (System Health, sibling AIPipelineRow) + weekly-report NFR3 breach flag + PRD NFR3 redefinition (v1.4, explicit_fp_rate<5%/class) + resolve-flow false_alarm hint. D1 reused shipped false_alarm tag (no enum/migration/contract change); D2 shipped 2 rates (auto-resolved-before-ack has no mechanism → dropped). CB unit 98 / CC vitest 257 / mypy --strict 37 files / ruff all GREEN; 5 integration tests → CI. security-sentinel APPROVED (aggregate-only endpoint, no PII/video/Conrad/secrets/injection). Next: code-review 10-5.
last_updated: 2026-06-14  # create-story (Amelia): 10-5-alert-quality-measurement → ready-for-dev. Final Epic-10 feature story. CODE-LIGHT: no shared/ change, no migration, no enum change. ENUM/PAYLOAD AUDIT caught 5 epic↔shipped divergences; user-confirmed D1 (reuse shipped false_alarm, no new false_positive tag) + D2 (drop dead auto-resolved-before-ack rate, ship 2 rates). New auth-guarded read-only /api/v1/ai-quality/resolution-rates endpoint + AIQualityRates CC component (sibling AIPipelineRow) + weekly-report NFR3 breach flag + PRD NFR3 redefinition (discharges prd.md:159 ⚠-pending + FR42 markers) + resolve-flow hint. 7 ACs, T1-T6 Tier 2. Next: dev-story 10-5.
last_updated: 2026-06-14  # code-review R1+R2 (Amelia): 6-4-vlan-pollers-fusion-context-contract → DONE. R1 (wf_caa5f4f5) caught a high — the nested-pis rewire defeated the 10-4 journey-change clear on the real wire (J2 inherited J1's stale departure). Fixed (fusion truthiness gate + vlan-pollers update_journey pis-reset); R2 re-review (wf_aad17dad) APPROVED — regression verified closed end-to-end, only one journey_id writer, no bypass, 12 confirmations/zero surviving. fusion 177 (93.74%) / vlan-pollers 91 (97.87%); mypy --strict + ruff clean. Commits ef25c75 + 940147f + final.
last_updated: 2026-06-14  # dev-story (Amelia): 6-4-vlan-pollers-fusion-context-contract → review. Option B reconciliation — fusion accepts the real full-delta /context body (typed PisPush + accept-ignore keys, extra='forbid' kept) → no more 422; scheduled_departure via nested pis (10-4 targeted push removed). Closes F21 (per-service isolation); 7-1 still owns DEFAULT_RETRY-on-4xx. fusion 176 (93.73%) / vlan-pollers 90 green; mypy --strict+ruff clean; security-sentinel APPROVED. Next: code-review 6-4.
last_updated: 2026-06-14  # create-story (Amelia): NEW 6-4-vlan-pollers-fusion-context-contract → ready-for-dev (Epic 6 Fusion Hardening). Surfaced by 10-4 review: vlan-pollers full-delta /context push 422s at fusion (extra='forbid' vs pis/trip_number/alarms/occupancy) → fusion gets no speed/occupancy/reservations/PIS in prod; inference accepts the same body, fusion is the outlier. D1 recommends Option B (declare real keys + typed pis sub-model, keep strict). Closes F21, de-dups 10-4 targeted push, cross-refs 7-1. Next: dev-story 6-4.
last_updated: 2026-06-14  # code-review Round 2 (Amelia, multi-agent adversarial wf_4b4c0912): 10-4 → DONE. Blocker verified CLOSED end-to-end on the real wire (seconds_to_departure=599 stamped via live producer→fusion→KPI replay); 17 fix-confirmations, all 10 R1 findings corroborated, zero regressions. 2 LOW stale comments fixed in place. APPROVED. Commits e90bcbc (dev) + c73677e (R1 fixes) + final comment cleanup.
last_updated: 2026-06-14  # code-review (Amelia, multi-agent adversarial wf_eb03b492): 10-4 → BLOCKED → in-progress. 10 findings all confirmed vs live code (1 blocker/2 high/2 medium/4 low). BLOCKER: scheduled_departure plumbing dead in prod (nested-pis vs flat-key + extra='forbid' 422) → KPI permanently empty; synthetic unit tests masked it. Fix cross-package (fusion ContextPushModel + real-producer contract test); scope decision pending. Review written to 10-4 story file (Round 1). Next: user decides fix scope, then re-dev.
last_updated: 2026-06-14  # dev-story (Amelia): 10-4-dwell-time-aware-alert-framing-and-kpi → review. seconds_to_departure (shared payload + fusion derivation/plumbing) + escalations col (Alembic 0008) + delay-minutes-avoided KPI endpoint + CC tile (browser-verified) + docs. shared 196 / fusion 170 (93.56%) / cloud-backend 176 / CC vitest 250 all green; mypy clean; security-sentinel APPROVED. Next: code-review 10-4 (different LLM).
last_updated: 2026-06-14  # create-story (Amelia): 10-4-dwell-time-aware-alert-framing-and-kpi → ready-for-dev. Multi-package code story; PAYLOAD AUDIT caught 4 epic↔shipped divergences (no DWELL state/event; escalation_audit lacks seconds_to_departure → +1 col on escalations Alembic 0008; Conductor App descoped Phase-2). 7 ACs, D1-D5 locked. Next: dev-story 10-4.
last_updated: 2026-06-14  # round-2 docs review + pilot-deps tracking (Claude): 10-3 STAYS done (deliverable met all 6 ACs). Fresh 5-angle adapted review caught 6 doc-defects (patched, commit 8e3edcb): enrichment.py severity-citation drift (:38/:30-45 → _severity_for :61-76), invented unattended_item → shipped unattended_bag, slip_fall medium-bucket 0.85 dual-match (Round-1 fixed only the legend, not the row), fernverkehr-onboard-first token drift, matrix→SOP token crosswalk, §3.4 dead-zone diagram over-promise bounded. 5 authoring gaps are ÖBB-owned PILOT gates, not story blockers → NEW epic-10-pilot-dependencies: open (source of truth = pilot-kickoff-checklist.md, now [BLOCKER]/[TUNABLE] tagged; added per-class response actions + life-safety closure rule + operator-training gate). Security-sentinel APPROVED. Also merged p95 ack-latency (10-2 spun-off task, commit 1e89c5b).
last_updated: 2026-06-14  # code-review + DONE (Claude): 10-3-critical-alert-sop-and-drill-cadence → done. Adapted docs review (3 adversarial layers + per-finding verification): 12 findings → 5 patched / 7 dismissed. Headline catch: SOP §3.4 "never silently dropped" was a FALSE safety guarantee — onboard event-store truncate_old_journeys(retain=3) purges by journey-recency not by cloud-sync acked prefix, so a >3-journey dead-zone outage can age out an un-pulled critical alert (two review layers split; settled empirically against cursor.py/pull_loop.py/ack_loop.py). Bounded to retention window + pilot-kickoff sizing dep. Other patches: confidence-bucket citation, door_fault not-shipped, bucket overlap, slip_fall sub-0.85 routing. Security-sentinel content sweep clean (no secrets/PII/raw-video; code-only checks N/A for docs). Commits f32e1c7 (dev) + 2a792e6 (review). Next P2: 10-4 dwell-time KPI.
last_updated: 2026-06-14  # dev-story (Claude): 10-3-critical-alert-sop-and-drill-cadence → review. DOCS-ONLY (Tier 2, no code) — code gates (RGB/pytest/mypy/ruff/Playwright/coverage/QA-score/sentinel) N/A per D1, adapted docs DoD applied. Authored 4 NEW md under _bmad-output/operational-procedures/ (critical-alert-sop, alert-routing-matrix, drill-cadence, pilot-kickoff-checklist) + "Closed by E10-S3" cross-link in gap analysis Gap 1. SOP grounded in shipped fusion _severity_for (door_obstruction/door_fault critical at speed>0/unknown; slip_fall). Matrix keyed on verified producer codes; fire/unattended_item = forward-looking (no producer). AC5/AC6 self-review clean (no stale tags/police/Conrad-as-human); all 11 cross-links resolve. Next: code-review 10-3 (docs review — different LLM).
last_updated: 2026-06-13  # create-story (Claude): 10-3-critical-alert-sop-and-drill-cadence → ready-for-dev. Docs-only Tier 2 story (4 NEW md files under _bmad-output/operational-procedures/ + gap-analysis cross-link). 6 ACs, 6 decisions. PAYLOAD-AUDIT (D2) caught epic↔producer divergence: shipped fusion codes are slip_fall/door_obstruction/door_fault (NOT epic's "fall"/"door-at-speed"); fire+unattended_item have NO shipped producer (forward-looking matrix rows). Corrected actor model (Conrad=platform not human; train-type-conditional ack). D5: project-actor-model-conrad memory EXISTS+indexed (canonical authority; shipped conrad_flag/SOURCE_LABEL naming is correct — do NOT rename). D6: train-type fleet-config source = open ÖBB dep (blocks populating, not authoring). Next: dev-story 10-3.
last_updated: 2026-06-13  # code-review (Amelia, 3 adversarial layers): 10-2 → DONE. Acceptance Auditor cleared all 6 ACs + 5 decisions (no AC gaps). 15 findings → 3 decision-needed (resolved: accept window-edge / atomic-insert / add pagehide), 7+2 patches ALL applied+verified, 4 deferred, 2 dismissed. Patches: CC dismissal-beacon misattribution (escalationId-in-closure, browser-verified A→B emits A's id), dwell_focus_ms INTEGER→BIGINT, action_tags jsonb_typeof guard, funnel half-open window, GREATEST ack-latency clamp, report (N/0) string, CLI ISO-week anchor, atomic dismissal insert (WHERE status='unacknowledged'), pagehide+visibilitychange emit (browser-verified, dedupe holds). cloud-backend 168 pass/82.58% cov, ruff+mypy --strict clean (31 files), CC 247 vitest. Next P0: 10-3 (Conrad/Claudia SOP + drill cadence) — backlog.
last_updated: 2026-06-13  # dev-story (Amelia): 10-2-operator-behavioural-telemetry → review. escalation_audit (Alembic 0007, append-only one-row-per-transition), audit-write hooks at all 3 transitions (rowcount-gated, INSERT…SELECT) + silently-dismissed endpoint (server-side unack re-check, 204), funnel GET /escalations-audit (DB-clock window + PERCENTILE_CONT + action_tag_distribution), CC dismissal beacon (keepalive fetch not sendBeacon — header auth; StrictMode guard; browser-verified both scenarios), weekly report callable + CLI + GitLab CI schedule (report stage), ADR-22. ROOT-CAUSED+FIXED real app↔DB clock-skew funnel undercount. cloud-backend 168 pass / 82.74% cov; CC 247 vitest pass; ruff+mypy --strict clean PACKAGE-WIDE (bg cleanup task cleared pre-existing debt incl preferences.py None-guard). GDPR: operator_id only. D3 taxonomy + GitLab Schedule UI config pending (non-blocking). Next: code-review 10-2 (different LLM), then 10-3.
last_updated: 2026-06-13  # dev-story (Amelia): 10-2-operator-behavioural-telemetry → in-progress (baseline 7afda88). escalation_audit (Alembic 0007), audit-write hooks at 3 transitions + silently-dismissed endpoint, funnel GET /escalations-audit, CC dismissal beacon, weekly report callable + GitLab CI schedule.
last_updated: 2026-06-13  # code-review (Amelia, 3 adversarial layers wf_e710dd45-3f5): 10-6 → DONE. 25 findings triaged vs live HEAD → 3 patches applied+verified (BLOCKER ESCALATION_UPDATED frame missing `id` for CC matching; concurrent-transition publish/log gated on rowcount; empty-payload ALERT_RAISED skips escalation upsert not 500), 3 deferred (dup-publish, SSE replay of ESCALATION_UPDATED, CC WS→SSE = E2-S1), 1 false-positive dismissed. cloud-backend 143 pass, CC 243 pass, ruff+mypy clean. Next P0: 10-2 (escalation_audit hooks these transitions).
# dev-story (Amelia): 10-6-escalation-lifecycle-persistence → review. escalations table (Alembic 0006), ingest ALERT_RAISED→escalation upsert, backend ack/resolve endpoints (404/409/422/idempotent) + ESCALATION_UPDATED SSE fan-out, CC acknowledge operator_id body + ACTION_TAGS replaced (4 landside tags). cloud-backend 142 pass (130→142), CC vitest 243 pass, ruff+mypy clean on new files, browser-verified. ADR-21 added. D3 taxonomy pending ÖBB (non-blocking). Next: code-review 10-6, then dev 10-2.
# create-story (Amelia, research wf_bb727ef4-f6e): NEW 10-6-escalation-lifecycle-persistence → ready-for-dev (prerequisite for 10-2 — builds E2-S5's missing backend ack/resolve endpoints + escalations table; without it 10-2's audit has no write-trigger). 10-2 → ready-for-dev with ACs rewritten to depend on 10-6; schema divergences resolved (alert_code/action_tags/model_versions-JSONB), report job = external GitLab CI schedule (no in-process scheduler infra). Sequence: 10-1(done) → 10-6 → 10-2. Decision B chosen (build backend lifecycle now during bench-hardware wait). ACTOR-MODEL CORRECTION 2026-06-13: no on-train conductor/Conrad, FM does not alert police/station — single landside actor (Fleet Manager); ack=action-taken, resolve=outcome. Stale ACTION_TAGS replaced with 4 decisive landside tags (resolved_remotely/field_team_dispatched/false_alarm/no_action_needed); 10-6 now also updates the shipped UI picker + tests (AC9). PoC default pending ÖBB confirm (non-blocking). Report job = GitLab CI schedule (confirmed GitLab CI/CD).
# 10-1 → DONE 2026-06-13. First integration run (Docker up): all 10-1-gating tests GREEN — killswitch fanout 3/3, heartbeat ingest, Alembic 0004+0005 idempotent; event-store integration 27/27. 4 unrelated pre-existing cloud-backend integration failures (:param::jsonb test-helper bug, baseline 9d4a60d) RESOLVED 2026-06-13 (task_cdd39efb, commit a96bd6f). task_6d7f88a2 spawned: audit inference/fusion producers stamp model_versions.
# code-review Round 3 decisions resolved: clou-2 /api/v1/health per-route auth (patched + test), clou-1 kill-switch clock-skew (accepted PoC/NTP, Known limitations), clou-3/4 cache asyncio.Lock + invalidate-generation guard (patched + 2 concurrency tests). cloud-backend unit 86 passed. All 4 review chunks now covered; all review patches applied + verified. Only remaining blocker to done: first CI run of integration tests + Alembic 0004/0005.
# code-review Round 3 (multi-agent workflow wf_96fe254e-e12): 10-1 chunks 2-4 (fusion/cloud-backend/control-centre — the original feature commit 80958e2, never reviewed; R1 covered chunk 1 only). 82 agents, 73 raw findings → 11 verifier-confirmed → reviewer re-verified vs live HEAD (caught 2 verifier false-positives on stale-diff CSS). NO Blocker/High survived. Outcome: 1 patch applied (fusi-1 door/slip-fall handler 500→202 + RED-verified test) + 1 self-caught R2 mypy-debt fix (ledger.py track_id|camera_id narrowing — R2 verification ran pytest not mypy --strict); 3 decision-needed (clou-2 /api/v1/health no-auth, clou-1 kill-switch clock-skew, clou-3/4 unlocked cache check-await-set); 2 defer (R3-D1 empty door_firmware_version, R3-D2 AIPipelineRow no-refresh); cont-2/4 amber-token finding REAL → fixed separately (commit 6387493, --obb-sev-warning→medium across 13 CC files). R2-D1 RESOLVED (commit 2cf0f89). fusion 156 passed cov 93.39% mypy --strict clean. Still in-progress: 3 R3 decisions + first CI run of integration tests + Alembic 0004/0005.
# code-review Round 2 (claude-fable-5): 10-1 → in-progress. Post-R1 delta (egress anonymisation + pipeline reconcile) reviewed by 3 adversarial layers. 3 decisions resolved (fail-closed egress, accept bbox/camera_id relaxation, 64-bit token), 4 patches applied + verified (P1 WAGON int|str token blocker + re-validation test, P2 WAGON camera_id drop, P3 fail-closed egress + drift tests, P4 64-bit token): shared 192 / event-store 84 / fusion 155 / inference 26 wagon — all green, mypy --strict clean. 3 defers (R2-D1 multiplexed-pipeline >1-cam NotImplementedError = hardware-bring-up blocker; R2-D2 no partial-batch flush; R2-D3 anonymise_page per-row isolation) in deferred-work.md. Held in-progress pending FIRST CI run of integration tests + Alembic 0004/0005.
# dev-story 2026-06-12 (Amelia): 10-1 → review. All 24 ACs; shared 172 / inference 159 (90.95%) / fusion 155 (94.15%, mypy --strict) / cloud-backend 83 unit / CC vitest 243 — all green. Integration tests + migrations 0004/0005 need first CI run (no local Docker). Deviations from AC text documented in story Completion Notes.
# dev-story 2026-06-12: 10-1-alert-confidence-and-ai-pipeline-health → in-progress (baseline 9d4a60d)
# code-review Round 1 2026-05-30: 1-6-prime-cloud-backend-sse-fanout patches applied (D-R1 SQL + D-R2 Alembic + P1-P12 + ingest.py timestamp bug fix); 66/66 tests pass
# create-story 2026-05-30: 1-6-prime-cloud-backend-sse-fanout → ready-for-dev (ratifies shipped alerts_sse.py against 7 BDD ACs from ADR-20; T1 adds event: field, T2 extends ALERT_EVENT_TYPES to luggage types, T3-T5 add integration test surface)
# code-review 2026-05-21 (opus-4.7): 1-5-3-onboard-docker-compose → done; D1 resolved (service_started), 7 patches applied (env_prefix fixes, api_key fields, restart, start_period); 149 inference + 43 rtsp-ingest tests pass
# code-review 2026-05-21 (opus-4.7): 1-5-2-rtsp-ingest-dockerfile → done; 4 patches applied (P1 env_prefix, P2 uvicorn host, P4 CLAUDE.md table); P3 dismissed; 5 deferred; 43 unit tests pass
# code-review 2026-05-21 (opus-4.7): 1-5-1-inference-dockerfile → done; 5 patches applied, 2 decisions resolved, 7 deferred; 149 unit tests pass
# dev-story 2026-05-21: 1-5-1-inference-dockerfile → review (Dockerfile + CLAUDE.md; docker build --check clean)
# create-story 2026-05-21: 1-5-1-inference-dockerfile → ready-for-dev (Hailo base image, CLAUDE.md, env-var config, CMD python -m inference.main)
# sprint-planning 2026-05-21: Phase 1.5 epic added (epic-1-5, 4 stories: inference/rtsp Dockerfiles + onboard compose + smoke test)
# create-story 2026-05-20: story 4-8 → ready-for-dev (gangway tripwire ingest); epic-5 → done
# bmad-code-review 2026-05-20 (opus-4.7): story 4-cs1 → done (2 BLOCKERs + 24 patches applied; 98 tests, 92.11% coverage; gates green)
# code-review 2026-05-20: story 4-5 → in-progress (BLOCKER: 2 tests fail; 6 patches + 7 decisions)
# dev-followup 2026-05-20: story 4-5 → review (all 20 R/F items addressed; 119 tests pass, coverage 91.03%)
# code-review 2026-05-20 (post-followup, opus-4.7): story 4-5 → done (APPROVED; S1 contract test added; S2/S3 deferred)
project: oebb-agent
# code-review 2026-05-17: story 2-2 → in-progress (1 patch finding: AC1 scroll-to-top)
# code-review 2026-05-17: story 2-3 → done (5 patches applied)
# dev 2026-05-18: story 2-7 → review (loading skeletons + Playwright E2E setup)
# dev 2026-05-18: story 2-8 → review (configurable alert threshold, preferences panel, backend router)
# create-story 2026-05-19: story 2-9 → ready-for-dev (system health data feed)
# code-review 2026-05-19: story 5-1 → done (8 patches applied)
# create-story 2026-05-19: story 5-2 → ready-for-dev (luggage monitoring live UI)
# create-story 2026-05-19: story 5-3 → ready-for-dev (luggage KPI live monitoring)
# create-story 2026-05-19: story 5-4 → ready-for-dev (luggage ISO timestamp migration)
# dev+review 2026-05-19: story 5-2 → done (luggage monitoring live UI, 11 patches applied)
# dev+review 2026-05-19: story 5-3 → done (luggage KPI live monitoring, 7 patches applied)
# dev+review 2026-05-19: story 5-4 → done (luggage ISO timestamp migration, 3 patches applied)
# create-story 2026-05-19: story 3-1 → ready-for-dev (analytics REST endpoints, backend)
# create-story 2026-05-19: story 3-5 → ready-for-dev (AI detection quality real data)
# create-story 2026-05-19: story 4-2 → ready-for-dev (APC/PIS/reservation pollers)
project_key: NOKEY
tracking_system: file-system
story_location: _bmad-output/implementation-artifacts

development_status:
  # ── Epic 1: Foundation & Shared Infrastructure ──
  epic-1: in-progress  # reopened 2026-05-30: E1-S6' added (cloud-backend SSE fan-out); was done
  1-1-e2e-skeleton-mvp: done
  1-2-event-envelope-pydantic-models: done
  1-3-postgresql-schema-alembic: done
  1-4-sqlite-event-store: done
  1-5-apc-adapter: done
  1-6-ws-subscription-spec: backlog  # ADR-20 (2026-05-30): re-scoped to onboard-only event-store WS; still absorbed by E4-S7; landside push moved to new 1-6-prime story below
  1-6-prime-cloud-backend-sse-fanout: done  # code-review Round 1 2026-05-30: D-R1+D-R2 resolved (SQL CTE rewrite + Alembic-driven test schema), P1-P12 patches applied, pre-existing ingest.py timestamp bug surfaced + fixed in scope; 66/66 tests pass; mypy + ruff clean on touched files
  1-7-rest-api-sse-fleet: done
  1-8-shared-contract-enforcement: done
  1-l1-landside-mqtt-ingestion: backlog  # MQTT ingestor deferred; 4-cs1 covers cloud-sync transport; full broker story pending
  epic-1-retrospective: done  # retro stays done; will redo at end of E1-S6' if substantial findings emerge

  # ── Epic 2: Control Centre Dashboard — Live Operations ──
  epic-2: done
  2-1-real-ws-client: done
  2-2-kpi-strip-filter-tap-wiring: done
  2-3-fleet-list-passenger-count-sort: done
  2-4-unified-feed-new-items-chip: done
  2-5-escalation-detail-acknowledge-resolve: done
  2-6-train-detail-event-store-alert: done
  2-7-loading-skeletons: done
  2-8-per-operator-configurable-alert-threshold: done
  2-9-system-health-data-feed: done
  epic-2-retrospective: done

  # ── Epic 3: Control Centre Dashboard — Analytics & System Health ──
  epic-3: done
  3-1-analytics-rest-endpoints: done
  3-2-capacity-exceptions-real-data: done
  3-3-occupancy-heatmap-real-data: done
  3-4-dwell-time-real-data: done
  3-5-ai-detection-quality-real-data: done
  3-6-system-health-websocket-staleness: done
  3-7-system-health-maintenance-ticket-api: done
  epic-3-retrospective: done

  # ── Epic 4: Onboard Edge Pipeline ──
  epic-4: done
  4-1-vlan-pollers-snmp-context-state: done
  4-2-vlan-pollers-apc-pis-reservation: done
  4-3-rtsp-ingest-camera-pipeline: done
  4-4-inference-detection-tracking-occupancy: done  # C1+F2+A+E+H+I+J+M patches applied 2026-05-19
  4-5-inference-safety-accessibility: done  # code-review 2026-05-20 (opus-4.7): APPROVED + S1 contract test
  4-6-fusion-alert-correlation-suppression: done
  4-7-event-store-onboard-rest-api-websocket: done
  4-cs1-cloud-sync-container: done
  4-8-gangway-tripwire-ingest: done  # code-review 2026-05-20 (opus-4.7): APPROVED after D1/D2 + P1-P7 patches; traversal rename, expect_orphan, 141 tests
  4-9-closed-ledger-reconciliation: done  # code-review 2026-05-21 (opus-4.7): 5 decisions resolved, 17 patches applied (enum alias, dedup, AC1 cleanup, AC8 + no-raw-video tests, startup-warn, etc.); 121 fusion tests, 149 inference, 130 shared; fusion cov 94.15%
  4-10-coach-comfort-index: done  # code-review 2026-05-21 (opus-4.7): 4 decisions (D1 accept, D2 accept+snapshot, D3 fix peek/consume, D4 accept), 5 patches (P1 confirm_emit two-phase, P2 journey reset, P3 edge suppression log, P4 clamp-on-store, P5 AC5/AC6 tests); 146 fusion tests cov 94.20%, mypy/ruff clean
  epic-4-retrospective: done  # phase-1 retro 2026-05-21 covering 4-1 through 4-8 + 4-cs1; phase-2 retro 2026-05-21 covering 4-9 + 4-10 — epic-4-retro-phase2-2026-05-21.md

  # ── Epic 5: Luggage Monitoring — Live Data ──
  epic-5: done
  5-1-luggage-ws-live-feed: done
  5-2-luggage-monitoring-live-ui: done
  5-3-luggage-kpi-live-monitoring: done
  5-4-luggage-iso-timestamps: done
  epic-5-retrospective: done

  # ── Epic 1.5: Onboard Containerised Infrastructure (Phase 1.5 bridge) ──
  epic-1-5: done
  1-5-1-inference-dockerfile: done          # containerise inference service; 5 patches applied (P1 uvicorn host, P3 API key docs, P4 device docs, P5 VOLUME, P7 py assertion); D1/D2 decisions documented
  1-5-2-rtsp-ingest-dockerfile: done        # containerise rtsp-ingest service; 4 patches applied (P1 env_prefix, P2 uvicorn host, P4 CLAUDE.md); 5 deferred; 43 unit tests pass
  1-5-3-onboard-docker-compose: done        # docker-compose.onboard.yml — 7 patches applied; 149 inference + 43 rtsp-ingest tests pass; compose config valid
  1-5-4-onboard-smoke-test: done            # code-review 2026-05-21 (opus-4.7): 7 patches applied; smoke test exits 0; cameras.json stub, CWD guard, curl body capture, journey_id filter, compose v2 check, API_KEY env, exec bit
  epic-1-5-retrospective: done              # epic-1-5-retro-2026-05-21.md; 5 action items; 23 patches total across 4 stories

  # ── Epic 10: Operator Adoption & Trust (AI PM gap-closure) ──
  # Source: _bmad-output/planning-artifacts/owning-the-gap-ai-pm-analysis.md (2026-05-30)
  # Pre-pilot blocker. Stories ordered by priority — implement in sequence (S1 → S6 → S2 → S3 → S4 → S5).
  # S1 scoped via grill-me + party-mode 2026-05-30: code-only, exec-failure playbook moved to S3, FP-rate measurement deferred to new S5.
  # S1 + S2 must land before Control Centre PostgreSQL contracts harden further.
  # 10-6 added 2026-06-13 via create-story (research wf_bb727ef4-f6e): backend escalation lifecycle persistence
  #   (the unbuilt backend half of E2-S5 — CC calls /escalations/{id}/acknowledge|resolve but no handler exists).
  #   PREREQUISITE for 10-2: 10-2's escalation_audit needs a server-side write-trigger. Sequence: 10-1 → 10-6 → 10-2.
  epic-10: in-progress
  10-1-alert-confidence-and-ai-pipeline-health: done   # P0 — DONE 2026-06-13. AlertRaisedPayload confidence metadata, INFERENCE_HEARTBEAT, fusion plumbing, cloud-backend kill-switch + endpoints, CC confidence chip + degraded banner + AI pipeline row. All 4 review chunks covered (R1/R2/R3); all decisions + patches resolved; R2-D1 RESOLVED. FIRST integration run (Docker, 2026-06-13) GREEN for all 10-1-gating tests: killswitch fanout 3/3 (AC11-14/ST5), heartbeat ingest (AC18), Alembic 0004+0005 apply cleanly & idempotently (test_upgrade_head_idempotent green); event-store integration 27/27. 4 unrelated PRE-EXISTING cloud-backend integration failures (:param::jsonb insert-helper bug, baseline 9d4a60d, untouched by 10-1) spun off → task_cdd39efb.
  10-6-escalation-lifecycle-persistence: done                  # P0 PREREQUISITE — escalations table + backend POST /escalations/{id}/acknowledge|resolve (E2-S5's missing backend half) + ALERT_RAISED→escalation upsert + SSE lifecycle fan-out + CC acknowledge operator_id body + REPLACE stale ACTION_TAGS (no conductor/Conrad/police/station — landside Fleet Manager only). New 4-tag taxonomy (canonical keys): resolved_remotely / field_team_dispatched / false_alarm / no_action_needed. Alembic 0006. Sequence before 10-2. PoC default pending ÖBB confirm (non-blocking).
  10-2-operator-behavioural-telemetry: done                    # P0 — escalation_audit (append-only, Alembic 0007) + GET /api/v1/escalations-audit funnels + silent-dismissal beacon (CC dismissal.js) + weekly effectiveness report (external GitLab CI schedule, no in-process scheduler). DEPENDS ON 10-6. Schema decisions: alert_code (not alert_class), action_tags (not outcome_tags), model_versions JSONB dict.
  10-3-critical-alert-sop-and-drill-cadence: done               # P1 — critical-alert SOP + routing decision matrix + drill cadence + exec-failure playbook (moved from S1). RE-SCOPED 2026-06-13 (actor model, memory project-actor-model-conrad): Conrad = virtual on-train platform (alert source, not human); ack authority train-type-conditional (conductorless=landside-only FM/remote staff; Fernverkehr=onboard conductor too). DROPPED: two-actor Conrad→Claudia sequence + ÖBB police/station security-handoff contract (out of PoC scope). Was: 10-3-conrad-claudia-sop-and-drill-cadence. STORY FILE created 2026-06-13 (create-story); docs-only Tier 2; D2 payload-audit caught epic↔producer code divergence (slip_fall/door_obstruction/door_fault shipped; fire/unattended_item not yet emitted); D5 flags dangling project-actor-model-conrad memory.
  10-4-dwell-time-aware-alert-framing-and-kpi: done            # P2 — DONE 2026-06-14. Round-2 re-review (wf_4b4c0912) APPROVED: blocker verified CLOSED end-to-end on the real wire (drove actual update_pis → real /context → seconds_to_departure=599 stamped through live producer→fusion→KPI chain); 17 fix-confirmations, all 10 Round-1 findings corroborated, zero regressions, zero new high/blocker. 2 surviving LOW = stale comments, fixed in place (models.py + context_state.py). Round-1 fixes applied (all 10 findings). BLOCKER fixed via targeted PIS push (vlan-pollers update_pis → fusion, mirrors set_station_approach; minimal-in-scope choice, strictly more surgical than fusion-pis-submodel — zero fusion-model change/blast-radius) + real-producer contract test bridging the wire. Journey-reset, AC2 degraded log, speed-branch future-gate, log snapshot, single-now boundary, AC4 prose, CC per-load AbortController all fixed. Pre-existing full-delta extra='forbid' 422 flagged (follow-up, not load-bearing). Gates green: vlan-pollers 89 / shared 176 / fusion 175 (93.68%, mypy --strict) / cloud-backend KPI 8 / CC vitest 250; browser re-verified. Round-1 review + resolution in story file. baseline ca4a219; dev commit e90bcbc + fix commit pending. CODE-REVIEW R1 (wf_eb03b492): BLOCKER was scheduled_departure plumbing dead in prod (nested-pis vs flat-key); synthetic unit tests masked it. PRE-FLIGHT FINDING: D1 understated fusion plumbing — fusion's ContextPushModel drops the pis data vlan-pollers sends, so fusion can't see scheduled_departure. User confirmed "build full story"; added D1-PLUMBING + task T1.5 (fusion-side ContextPushModel/ContextState field, no vlan-pollers change). create-story 2026-06-14 (Amelia). Multi-package code story (shared payload Tier-3 + fusion derivation + cloud-backend KPI endpoint + CC tile + Conductor-App spec stub). PAYLOAD AUDIT caught 4 epic↔shipped divergences (D1-D4): NO DWELL state (use shipped station_approach + PisState.scheduled_departure — already polled); NO DWELL_FEED_DEGRADED EventType (reuse graceful pis-degrade log; seconds_to_departure=None); escalation_audit has NO seconds_to_departure col → add 1 additive col on escalations (Alembic 0008) keyed off the 10-6 upsert; Conductor App descoped to Phase 2 (CC tile read-only via new REST endpoint, NOT SSE kpis). 7 ACs. No ADR change (additive optional field doesn't contradict ADR-20/21/22; ADR ceiling=22). Next: dev-story 10-4.
  10-5-alert-quality-measurement: done                          # P3 — DONE 2026-06-14 (Amelia). Code-review R1 (wf_4b011970, 14 agents) CHANGES_REQUESTED → R2 (wf_0c944bdb) APPROVED. R1: completeness critic caught a BLOCKER all 4 find-layers missed — integration tests violated escalation_audit→escalations FK (orphan escalation_id) → all 4 data tests would error on CI, ?-operator SQL had zero passing coverage (green-test trap). Fixed via _seed_parent. HIGH: AIQualityRates.test.jsx was untracked (absent from ff96e4a) → committed. MEDIUM: ?-operator verified correct (Postgres source via Context7). LOW×2: 2-decimal label (gate/tint consistent at 5% boundary)+boundary test; _render NFR3 section tests. 6 refuted. R2: all 3 fixes re-verified + 1 new low (fixture leaked escalations parents) fixed. Gates: CB unit 100 / CC vitest 258 / mypy --strict 37 / ruff clean. RESIDUAL (CI-gated, not defects): integration run needs Docker (same as 10-1/10-4); browser-verify open (pre-existing mock-config SPA-mount block). Commits ff96e4a (dev) + 9e280a3 (R1 fix) + R2 fix. dev-story 2026-06-14 (Amelia, baseline e42a912). CODE-LIGHT, fully additive: new read-only GET /api/v1/ai-quality/resolution-rates (2 rates/class off escalation_audit, 7-day skew-proof window, auth-guarded) + AIQualityRates CC component (sibling AIPipelineRow, --obb-* only, % (n of d), breach tint) + weekly-report NFR3 breach section (_nfr3_breaches) + PRD NFR3 redefinition (v1.4) + resolve-flow hint. D1 reused shipped false_alarm (NO new tag/enum/migration); D2 shipped 2 rates (dropped dead auto-resolved-before-ack). Gates GREEN: CB unit 98 / CC vitest 257 (7 new) / mypy --strict 37 files / ruff clean. Integration suite (5 tests) written → CI (no local Docker, same as 10-1/10-4). Browser verify blocked by pre-existing mock-config SPA-mount failure (not this story; Vite built clean). security-sentinel APPROVED. Next: code-review 10-5 (different LLM). CODE-LIGHT (no shared/ change, no migration, no enum change). ENUM/PAYLOAD AUDIT caught 5 epic↔shipped divergences; 2 load-bearing resolved by user decision: D1 reuse shipped false_alarm (NOT a new false_positive tag — 10-6's ACTION_TAG_KEYS already encodes false_alarm as the FP signal + action_tags is min_length=1, so a new exclusive tag duplicates + contradicts the contract); D2 drop auto_resolved_before_ack_rate (no auto-resolve path exists → structurally 0) → ship TWO rates (no_action_rate + explicit_fp_rate). D3 new AIQualityRates component sibling to AIPipelineRow (10-1 drawer is per-train, no per-class). D4 rates computed from existing escalation_audit columns, 7-day skew-proof window reusing escalations_audit idioms. D5 PRD NFR3 row at prd.md:159 + FR42 already await this story. 7 ACs, T1-T6 all Tier 2. Discharges the NFR3 ⚠-pending + FR42 markers. Next: dev-story 10-5.
  # 10-3 is DONE (deliverable met all 6 ACs) but surfaced ÖBB-owned PILOT gates — NOT story blockers, NOT reasons to reopen 10-3.
  # These block PILOT GO-LIVE, not dev. Source of truth + checkboxes: _bmad-output/operational-procedures/pilot-kickoff-checklist.md
  # Tracked here so they're visible at sprint level. [BLOCKER] = go-live blocker; [TUNABLE] = confirm-but-non-blocking PoC default.
  epic-10-pilot-dependencies: open  # owner: ÖBB ops (via Nomad AI PM at kickoff prep). See pilot-kickoff-checklist.md
  #   [BLOCKER] SOP signed off · routing matrix signed off · operators trained on SOP · first tabletop drill done
  #   [BLOCKER] ÖBB on-call escalation path defined (SOP §3.3 last line of defence)
  #   [BLOCKER] per-class operator response actions defined (Round-2: SOP routes/tags but doesn't say what to DO)
  #   [BLOCKER] life-safety closure rule (no field-verify rule before closing fire/slip_fall false_alarm/no_action_needed)
  #   [BLOCKER] onboard retention window sized vs worst-case dead-zone (truncate_old_journeys retain=3; SOP §3.4 silent-drop edge)
  #   [TUNABLE] train-type fleet-config source (D6; pick safe default unknown→conductorless) · amber-window value (D4; consider shorter for life-safety) · live-drill scheduled
  epic-10-retrospective: done  # 2026-06-14 — epic-10-retro-2026-06-14.md. Headline: 5 of 6 stories carried a BLOCKER into review, all the SAME species (test/prod divergence — green tests not exercising the real path); all caught by discretionary adversarial-wire review. 4 action items: A1 HARD integration-before-review gate (would have killed 3 of 5 blockers at the dev desk), A2 risk-tier the review (light for docs/additive, full wire-replay for contract/migration/auth), A3 create-story divergence rule (seed via real producer not raw inserts), A4 fix control-centre-mock launch config (VITE_MOCK_API is dead; real seam is VITE_WS_URL — caused 10-5 browser-verify misdiagnosis). Epic-4 A1 lesson (ADR-as-checkbox) confirmed applied (10-6 shipped ADR-21 in-commit). Next: wire A1-A3 into harness configs before first Epic-11 (Auth/Identity) dev-story; Epic 11 needs a planning pass (placeholder → breakdown, verify X-API-Key→JWT seam compat first).

  # ── Epic 11: Control Centre Admin & Identity ──
  # Source: surfaced 2026-05-30 during E10-S1 grill-me. Prerequisite for the UI half of E10-S1's kill-switch + threshold mutability.
  # E10 work does NOT block on Epic 11 — E10-S1 uses an X-Admin-Key seam designed to be forward-compatible with JWT auth.
  # PLANNING PASS 2026-06-14 (Saga, bmad-create-epics-and-stories): placeholders → full breakdown in epics.md §"Epic 11".
  #   Auth decision: SELF-CONTAINED JWT for PoC (local users table + passlib/bcrypt; cloud-backend issues+verifies HS256).
  #   NO external IdP. Keycloak / ÖBB-OIDC SSO = Phase 2, explicitly out of scope. ADR-23 (authored by E11-S1) records this +
  #   the verification-seam contract: token verification isolated behind get_current_user, decoupled from issuance, so the
  #   13 protected routers never change when SSO swaps in later (avoids building the integration twice).
  #   Seam compat VERIFIED against live code first (epic-10-retro prerequisite). Shipped-seam facts: NO get_current_user today
  #   (cloud-backend/CLAUDE.md is aspirational); auth = 2 flat key schemes (X-API-Key on 14 routers + X-Admin-Key on 1);
  #   operator_preferences keyed by the API-KEY STRING (E11-S3 carries a re-key migration, not a clean extract); no auth libs
  #   installed; migrations run 0001→0008 so users table = 0009. Retro action items wired: A1 hard integration gate +
  #   A2 risk-tier review (auth+migration ⇒ FULL adversarial wire-replay for S1-S4) + A3 seed-via-real-path in every integration test.
  #   Sequence: S1 → S2 → (S3 ∥ S4) → S5. Stories below are PLANNED (broken down) but NOT ready-for-dev.
  #   Next: create-story E11-S1 in a fresh context (verify staged router-cutover vs main.py mounting before coding).
  epic-11: in-progress  # first story (11-1) created 2026-06-14; breakdown done same day
  11-1-jwt-auth-foundation-login: done                          # DONE 2026-06-14 (Amelia). code-review R1 (FULL adversarial wire-replay: Blind + Edge-Case + Acceptance-Auditor, 3/3) APPROVED — all 9 ACs SATISFIED, no test/prod-divergence, security core cleared by all 3 layers (verify allow-list+issuer+require-claims+fail-closed, both extractors share _verify_token, no user-enum oracle, 16-surface cutover complete, A3 real-create_user seed). 2 decision-needed RESOLVED + 6 patches APPLIED: (D1) ingest /api/v1/events carved back to require_api_key — unattended machine producer, human JWT wrong model; the ONE documented AC5 exception; service-token = follow-up. (D2) SSE ?token= accepted for PoC, ADR-23 notes deployment log-scrub. Patches: username null-coercion (or ""); migration test now genuinely downgrade→upgrade re-applies 0009 + schema asserts; SSE ?token= positive-auth test; Test-7 timing spy (verify_password runs on unknown user); confidenceThresholds rejected-promise; 0009 docstring. POST-PATCH GATES GREEN: CB 114 unit + 116 integration on real PG (A1 ran locally, Docker up), mypy --strict 38 files, ruff clean; CC 264 vitest. Self-contained JWT: _verify_token core + 2 extractors; bcrypt DIRECT; uniform-timing login; require_role; Alembic 0009. Frontend: Login + RequireAuth guard + tokenStore(sessionStorage) + authFetch(Bearer+401) + AuthContext; all consumers → Bearer. ADR-23 (+ADR-7/22) + CLAUDE.md. Browser-verified AC6. security-sentinel APPROVED (caught+fixed dismissal-beacon Major). DEFERRED (pre-existing, NOT from 11-1 — reproduced at 718e56f): integration-test DATABASE_URL isolation flaky (3 ai_quality_rates tests under multi-module ordering) → task_c97431d4. Tier 3, default mode. Depends: —. Next: 11-2.
  11-2-user-management: done                                   # PLANNED. admin-only CRUD users + role assignment + deactivate + password reset, audited; frontend Users screen (admin-gated). Tier 3. FULL review. Depends: 11-1
  11-3-profile-management-prefs-rekey: done                    # DONE 2026-06-15 (Amelia). code-review R1 (FULL adversarial wire-replay: Blind + Edge-Case-with-REAL-wire-replay + Acceptance-Auditor) APPROVED + security-sentinel APPROVED. No behaviour violation — every AC/Decision correct; the Edge Case Hunter EMPIRICALLY replayed the migration on realistic pre-migration data (non-castable rows, uuid-lookalike orphans, zero-rows, downgrade→upgrade re-apply, ON DELETE CASCADE) and the live login→PATCH→GET wire with no 500s, and resolved 6 of the Blind Hunter's out-of-diff guesses as false. 1 decision (avatar)→KEEP. 3 mandated-test patches applied (junk-sub SQL-safety, expired/tampered on both verbs, caplog deleted-count). Security-Test-2 SURFACED A REAL LATENT BUG (pre-existing from E11-S2, NOT 11-3): a non-UUID `sub` 500'd at the liveness `WHERE user_id=:uid` bind instead of 401 (the uuid column rejects the bind) — FIXED via try/except DBAPIError→401 in assert_user_active (also removes a 500-stack-trace info-leak; sentinel noted this IMPROVES posture). First fix attempt (pre-validate UUID) regressed 7 unit tests minting u-operator/u-admin sentinel subs vs mocked DBs → caught by the full-suite gate → re-fixed by catching the bind error (mocked tests never raise it). POST-PATCH GATES GREEN: backend 275 passed (unit+integration real PG, A1 gate, deterministic ×2), mypy --strict clean (40), ruff clean, cov 82.37% (preferences.py 100%); frontend 281 vitest. Sentinel: no Critical/Major; §1 authz / §5 secret-hygiene / §6 injection / §8 log-safety / §10 frontend all PASS; video/Hailo/Conrad/SSE N/A. Epic 11: 3 of 5 stories done (S1,S2,S3); S4 unblocked (S3∥S4). dev-story DONE (Amelia) 2026-06-15 — all 6 ACs, baseline a7e6a2d. 0011 re-key migration (DELETE defunct API-key rows + log.warning count → ALTER operator_id TYPE uuid USING ::uuid → ADD FK users(user_id) ON DELETE CASCADE; one transaction = atomic). REAL BUG caught by the A1 integration gate (the headline): post-0011 the raw-SQL RETURNING operator_id yields a Python UUID object but PreferencesOut/PreferencesPatchOut.operator_id are str-typed → Pydantic 500 on EVERY GET/PATCH; a mocked unit test would've stayed green. Fixed by str() coerce at both return sites (model stays str per Winston's type-seam ruling; column is uuid). Frontend: SegmentedControl extracted to shared file (modal tests stay green — zero focus-trap change), formatSec→constants/preferences.js, new /dashboard/profile route (getUsername added to tokenStore+AuthContext; 2 server-backed controls; unattended_threshold_min deliberately OFF Profile per D5 clash-resolution — tracked E11S3-D5). D4 api_key STAYS (live on ingest:29) + both stale comments fixed + Phase-2 service-identity follow-up tracked (E11S3-D4). GATES: backend 273 passed (unit+integration real PG via testcontainers — A1 gate RAN locally, Docker up), mypy --strict clean (40), ruff clean, cov 82.46% (preferences.py 100%); frontend 281 vitest (275 baseline + 6 new). BROWSER-VERIFIED AC4 server persistence by clearing localStorage between change+reload (UI→60 → DB shows 60 → clear localStorage → reload → 60s active from server GET); unauth /dashboard/profile → /login (AC5); only 2 controls (no unattended). Console clean except pre-existing mock-train fetchTrainAlerts 404s. eslint NET-ZERO new violations (refs-during-render relocated verbatim from OperatorPreferences; only-export-components avoided via formatSec move; AuthContext one is pre-existing per 11-2). FULL adversarial wire-replay review + security-sentinel next. 2026-06-15 (create-story, Amelia). re-key operator_preferences from api-key-string-keyed TEXT → UUID FK to users(user_id). GROUNDED vs 11-1: identity-swap ALREADY done (preferences.py keys by current_user.user_id; frontend PATCHes via Bearer) → residual = SCHEMA re-key migration + FK + Profile route. 5 Decisions: D1 drop/seed (DELETE defunct rows, no fabricated 1-key→N-user mapping); D2 migration is 0011 NOT 0010 (0010=user_audit shipped by 11-2 — epic stale); D3 in-place DELETE→ALTER COLUMN TYPE UUID→ADD FK ON DELETE CASCADE, DELETE BEFORE ALTER (string→uuid cast on real defunct data fails otherwise); D4 api_key/require_api_key STAYS (11-1 carved POST /api/v1/events back to it — config:19 comment stale, fix not remove); D5 Profile REUSES gear-modal SegmentedControl (no rebuild) + unattended_threshold_min localStorage-only → out of scope. 6 ACs (AC1 two-user isolation; AC3 migration drops defunct non-UUID rows + idempotent; AC5 logged-out 401). 3 RED security tests (no client operator_id; uuid SQL-safety; logged-out 401). A1 gate: 2 real users via create_user (A3) + FK-rejects-unknown + rekey-drops-defunct + 0010→0011 re-apply. Tier 3 (migration on existing data), default mode. FULL review. Browser-verify. Depends: 11-1, 11-2 (done). NOT yet dev'd.
  11-4-admin-ui-alert-class-killswitch: ready-for-dev          # 2026-06-15 (create-story, Amelia). Epic-11 S4 (S3∥S4; S1+S2+S3 done). Auth swap on the LIVE E10-S1 kill-switch — genuinely a SINGLE clean swap point (router-level dep admin_alert_classes.py:45 require_admin_key→require_role('admin'), mirrors users.py exactly). ONE load-bearing Decision RESOLVED (user): actor source → current_user.username (display-legible in disabled_by/the UI), AdminActionBody.actor_name REMOVED (empty-body POST, no body-spoof path). PAYLOAD AUDIT cleared: AlertClassStatePayload.actor_name stays (_NonEmptyStr) — only the VALUE source changes (body→token), NO shared edit/contract test/migration. ADR FRESHNESS cleared: ADR-23 already anticipates the X-Admin-Key retirement, no ADR contradicted. cc_admin_key REMOVED (zero readers after the swap — symmetric-opposite of 11-3 D4 where api_key STAYS for ingest). Frontend = clean mirror of Users (NEW AlertClasses.jsx + api/alertClasses.js + test; modify App.jsx+AppShell.jsx) — ZERO existing kill-switch caller. 6 ACs (AC1 fan-out suppression preserved end-to-end through the new auth path = the core guarantee; AC2 operator 403/admin 200/no-token 401; AC3 actor=token.username, no body spoof; AC4 old X-Admin-Key 401). 4 RED-first security tests (operator 403, old-key-no-bearer 401, auth≠authz, body-actor ignored). A1 gate: real admin/operator via create_user → JWT toggle → ingest a new alert → assert suppressed (not synthetic state poke). Tier 3 (auth swap on live kill-switch), default mode. FULL adversarial wire-replay review. Browser-verify. Depends 11-1+11-2 (done). NOT yet dev'd.
  11-5-configuration-surface: backlog                           # PLANNED. mutable confidence thresholds (persist + admin PATCH; Alembic 0011 if table; seed CALIBRATE defaults; gate reads persisted), admin Config screen. Per-operator alert threshold already re-keyed by 11-3. Tier 2→3 if migration. LIGHT→FULL review. Depends: 11-1, 11-3
  # UX: each story ships MINIMAL functional UI (theme-correct, browser-verified) so the backend critical path isn't gated on design.
  # Freya runs ONE coherent WDS UX pass over the whole Epic-11 admin/identity surface (login + logout + role-gated nav + S2-S5 admin shell)
  # once S2-S5 screens exist — tracked epic-exit step (avoids redesigning login in isolation). See epics.md §Epic 11 "UX pass (Freya)".
  epic-11-retrospective: optional

  # ── Epic 6: Fusion Hardening — Journey Lifecycle & Handler Robustness ──
  # Source: deferred items from E4-S9, E4-S10, E4-S6 code reviews
  # Containers: fusion/
  # FRs covered: NFR1 (uptime), NFR3 (false-positive rate)
  epic-6: backlog
  6-1-fusion-journey-lifecycle-reset-hook: backlog              # reset CoachLedger / ComfortIndex / SuppressionGate per-journey state; tests/unit/test_journey_lifecycle.py
  6-2-fusion-handler-async-exception-hardening: backlog         # /candidates/* handlers catch asyncio.TimeoutError + ValidationError → 200/422 not 500
  6-3-inference-test-hygiene-asyncmock-warnings: backlog        # eliminate 5 "coroutine never awaited" warnings in inference unit tests
  6-4-vlan-pollers-fusion-context-contract: done               # DONE 2026-06-14. Round-2 re-review (wf_aad17dad) APPROVED: journey-change stale-departure regression verified CLOSED end-to-end on the real wire (J2 push carries empty pis via update_journey reset — only one journey_id writer, no bypass; fusion truthiness gate clears). 12 confirmations, zero surviving problems, zero new regressions. fusion 177 (93.74%) / vlan-pollers 91 (97.87%); mypy --strict + ruff clean. Commits ef25c75 (impl) + 940147f (R1 fix). code-review R1 (wf_caa5f4f5) CHANGES_REQUESTED (1 high) → FIXED → back to review. R1 (high): nested-pis rewire defeated the 10-4 journey-change clear on the REAL wire (producer always sends pis; update_journey didn't reset _state.pis → J2 inherited J1's stale departure). FIX: fusion truthiness gate (if new_dep:) + vlan-pollers update_journey resets _state.pis; re-verified end-to-end (no leak). R2 test re-pinned to real wire; R3 empty-pis keeps-prior; R4 stale comments. Option B reconciliation stands (full-delta 200 not 422; per-service isolation closes F21; D2→7-1). Security-sentinel APPROVED. fusion 177 (93.74%) / vlan-pollers green; mypy --strict + ruff clean. baseline c2db726. D4 resolved (comfort_index gets occupancy via /candidates/occupancy_update). NEW 2026-06-14 (create-story, Amelia) — surfaced by 10-4 review. vlan-pollers full-delta /context push 422s at fusion (extra='forbid' rejects pis/trip_number/alarms/occupancy) → fusion never gets speed/occupancy/reservations/PIS in prod. Inference accepts the same body (extra='ignore') — fusion is the outlier. Reconcile the contract (D1: recommend Option B — declare real producer keys + typed pis sub-model, keep extra='forbid' for genuinely-unknown). Closes F21 (per-service error isolation); de-dups 10-4 targeted PIS push (D3); cross-refs 7-1 (DEFAULT_RETRY-on-4xx, NOT fixed here). 6 ACs, D1-D4. Next: dev-story 6-4.
  epic-6-retrospective: optional

  # ── Epic 7: Retry & Idempotency Hardening ──
  # Source: deferred items from E4-S8 (W1, W3), E4-S6, E4-CS1 code reviews
  # Containers: shared/, inference/, cloud-sync/
  # FRs covered: NFR1 (uptime), NFR8 (connectivity resilience)
  epic-7: backlog
  7-1-shared-retry-policy-exclude-4xx: backlog                  # DEFAULT_RETRY: no retry on 4xx (except 429); contract test
  7-2-gangway-tripwire-emit-then-commit: backlog                # tripwire records _last_side only after successful emit; journey_id snapshot at detection time
  epic-7-retrospective: optional

  # ── Epic 8: Analytics UI Hardening ──
  # Source: deferred items from E3-S3..S7, E2-S2, E5-S1 code reviews
  # Component files: src/components/analytics/, src/context/FleetContext.jsx
  # FRs covered: UX-DR8–UX-DR12, NFR1
  epic-8: in-progress  # 2026-06-14: first story created (8-3 SSE-client migration) — was backlog
  8-1-abortcontroller-analytics-fetch-cancellation: backlog     # cancel stale fetches on range change + unmount; 300ms debounce on retry
  8-2-fleetcontext-provider-memoisation: backlog                # useMemo on context value; eslint exhaustive-deps clean
  8-3-landside-sse-client-migration: ready-for-dev              # NEW 2026-06-14 (create-story, Amelia). Fixes pre-existing landside live-transport mismatch (surfaced in 11-1 D8). Implements the never-built post-ADR-20 E2-S1 (epics.md:517-551): new src/sse/RealSseClient.js (EventSource → /api/v1/alerts/stream, named-event listeners per ALERT_EVENT_TYPES, Last-Event-ID native reconnect, ported _handleEnvelope/dedup/normalisation), repoint FleetContext, DELETE dead RealWebSocketClient.js+test (D2 clean-delete not epics.md:547 stub — only FleetContext imports it), rename VITE_WS_URL→VITE_SSE_URL. Build-now vs require_api_key; _buildUrl() isolated so 11-1 adds ?token= in 1 line (EventSource no-header → 11-1 D8). ADR-20:642 ❌→✅ + 2-1 superseded note (cross-ref freshness, no ADR decision change). 8 ACs, FULL review (SSE wire contract; D9 WS≠SSE frame = green-but-wrong trap), Tier 2, browser-verify. Depends: — (11-1 independent; coordinated via D3/D8). Next: dev-story 8-3.
  epic-8-retrospective: optional

  # ── Epic 9: Container & Infrastructure Hardening ──
  # Source: deferred items from E1-S1, E1-S3, E1-S4, E4-CS1, E1.5-1..3 code reviews
  # Containers: all Dockerfiles, cloud-backend/ migrations
  # FRs covered: NFR1 (uptime), NFR13 (CI/CD)
  epic-9: backlog
  9-1-postgresql-analytics-indexes: backlog                     # 0002_add_analytics_indexes.py — (timestamp,event_type), (vehicle_id,timestamp), journeys.vehicle_id
  9-2-dockerfile-hardening-pass: backlog                        # non-editable pip installs, HEALTHCHECK directives, Hailo digest-pin comment, FastAPI lifespan migration
  9-3-cloud-sync-batch-commits-bg-truncation: backlog           # 50ms batched commits; asyncio.create_task for truncate_old_journeys
  epic-9-retrospective: optional
